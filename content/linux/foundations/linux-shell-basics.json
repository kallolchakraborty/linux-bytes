{
  "id": "linux-shell-basics",
  "title": "Shell Scripting Basics",
  "category": "Linux",
  "subcategory": "Foundations",
  "language": "bash",
  "description": "Shell scripting is how Linux systems are automated. From CI/CD pipelines to deployment scripts to cron jobs — if you work at FAANG, you will read and write shell scripts. This guide covers the essentials with a focus on patterns that matter in production environments.",
  "codeBlock": "",
  "sections": [
    {
      "title": "Script Anatomy & Shebang",
      "description": "Every shell script starts with a shebang (<code>#!/bin/bash</code> or <code>#!/bin/sh</code>) and should set strict error handling. The shebang tells the kernel which interpreter to use. In FAANG production scripts, you'll almost always see <code>#!/bin/bash</code> with <code>set -euo pipefail</code>.",
      "codeBlock": "#!/bin/bash\n# The standard FAANG production header:\nset -euo pipefail\n# -e:  Exit on error (any command fails)\n# -u:  Treat unset variables as error\n# -o pipefail: Pipeline fails if any command fails (not just the last)\n# -x:  Print each command before executing (for debugging)\n\n# Making your script executable:\nchmod +x script.sh\n./script.sh          # Run it\nbash script.sh       # Also works\n\n# FAANG note: Never use #!/bin/sh in production\n# On Ubuntu/Debian, /bin/sh is dash (minimal POSIX)\n# On RHEL/CentOS, /bin/sh is bash\n# Use bash explicitly to avoid compatibility surprises"
    },
    {
      "title": "Variables, Parameters & Quoting",
      "description": "Variables, command-line arguments, and proper quoting are the foundation. <strong>Always quote your variables</strong> — unquoted variables undergo word splitting and glob expansion, causing bugs that are hard to debug. This is a FAANG interview red flag when missed.",
      "codeBlock": "#!/bin/bash\nset -euo pipefail\n\n# Variables — no spaces around =\nNAME=\"World\"\necho \"Hello, $NAME\"          # Variable expansion\necho \"Hello, ${NAME}!\"       # Braces for clarity/concatenation\n\n# Command-line arguments\necho \"Script: $0\"            # Script name\necho \"First arg: $1\"         # $1-$9 are positional args\necho \"All args: $@\"          # All args as separate words\necho \"Arg count: $#\"         # Number of args\nshift                        # Shift $2→$1, $3→$2, etc.\n\n# Special variables\necho \"Exit code: $?\"          # Exit code of last command\necho \"PID: $$\"               # Current script PID\necho \"Last BG PID: $!\"       # PID of last background process\n\n# QUOTING — this will bite you in production:\nFILE=\"My Document.txt\"       # Filename with space\ncat $FILE                     # ERROR: tries to cat 'My' and 'Document.txt'\ncat \"$FILE\"                  # CORRECT: passes the whole filename\n\n# Using defaults\necho \"${NAME:-default}\"       # Use default if unset\necho \"${NAME:=default}\"       # Set default AND use it\n\n# Command substitution (two styles)\nCURRENT_DIR=$(pwd)            # Modern style (preferred)\nCURRENT_DIR=`pwd`             # Old style (avoid)\n\n# Arithmetic\nCOUNT=$((COUNT + 1))          # $(( ... )) for integer math"
    },
    {
      "title": "Conditionals & Control Flow",
      "description": "The <code>if</code>/<code>test</code>/<code>[ ]</code>/<code>[[ ]]</code> system is unlike any other language. The <code>test</code> command (or <code>[</code>) checks conditions. The double-bracket <code>[[ ]]</code> is a Bash extension with more features (pattern matching, regex).",
      "codeBlock": "#!/bin/bash\nset -euo pipefail\n\n# File tests (most common in scripts)\nif [ -f \"$FILE\" ]; then echo \"$FILE exists\"; fi\nif [ -d \"$DIR\" ]; then echo \"$DIR is a directory\"; fi\nif [ -x \"$BINARY\" ]; then echo \"$BINARY is executable\"; fi\nif [ -z \"$STRING\" ]; then echo \"String is empty\"; fi\nif [ -n \"$STRING\" ]; then echo \"String is not empty\"; fi\n\n# String comparison\nif [ \"$NAME\" = \"root\" ]; then echo \"Is root\"; fi     # POSIX: single =\nif [ \"$NAME\" != \"admin\" ]; then echo \"Not admin\"; fi\nif [[ \"$NAME\" == r* ]]; then echo \"Starts with r\"; fi  # Pattern match (Bash)\nif [[ \"$HOST\" =~ ^web[0-9]+ ]]; then echo \"Web server\"; fi  # Regex match\n\n# Numeric comparison\nif [ \"$COUNT\" -gt 10 ]; then echo \">10\"; fi          # -gt, -lt, -ge, -le, -eq\nif (( COUNT > 10 )); then echo \">10\"; fi             # Modern math syntax\n\n# File descriptors in conditionals\nif [ -t 0 ]; then echo \"stdin is a terminal\"; fi     # Interactive session?\n\n# elif and else\nif [ \"$1\" = \"start\" ]; then\n    /usr/bin/myservice start\nelif [ \"$1\" = \"stop\" ]; then\n    /usr/bin/myservice stop\nelse\n    echo \"Usage: $0 {start|stop}\"\n    exit 1\nfi\n\n# case statement — cleaner than long if-elif chains\ncase \"$1\" in\n    start|restart)   action=\"start\" ;;\n    stop)            action=\"stop\" ;;\n    *)               echo \"Unknown: $1\"; exit 1 ;;\nesac"
    },
    {
      "title": "Loops & Arrays",
      "description": "Loops iterate over files, commands, or sequences. Arrays are underused in shell scripts — they make scripts cleaner and more maintainable.",
      "codeBlock": "#!/bin/bash\nset -euo pipefail\n\n# for loop over explicit list\nfor fruit in apple banana cherry; do\n    echo \"$fruit\"\ndone\n\n# for loop over command output\nfor file in /etc/*.conf; do\n    echo \"Found: $file\"\ndone\n\n# for loop with seq (C-style)\nfor ((i=0; i<10; i++)); do\n    echo \"Number $i\"\ndone\n\n# while loop — read file line by line\nwhile IFS= read -r line; do\n    echo \"Line: $line\"\ndone < input.txt\n# IFS= preserves leading whitespace\n# -r prevents backslash interpretation\n\n# Arrays\nFRUITS=(\"apple\" \"banana\" \"cherry\")\necho \"${FRUITS[0]}\"          # First element\necho \"${FRUITS[@]}\"          # All elements\necho \"${#FRUITS[@]}\"         # Array length\nFRUITS+=(\"date\")             # Append\n\n# Iterate array\nfor fruit in \"${FRUITS[@]}\"; do\n    echo \"$fruit\"\ndone\n\n# Associative arrays (Bash 4+)\ndeclare -A COLORS\nCOLORS[apple]=\"red\"\nCOLORS[banana]=\"yellow\"\necho \"${COLORS[apple]}\"       # red"
    },
    {
      "title": "Functions & Error Handling",
      "description": "Functions organize reusable logic. Error handling patterns with trap ensure cleanup on failure — this is non-negotiable in FAANG production scripts.",
      "codeBlock": "#!/bin/bash\nset -euo pipefail\n\n# Define a function\nlog() {\n    local level=\"$1\"        # local makes variable function-scoped\n    local message=\"$2\"\n    echo \"[$(date '+%Y-%m-%d %H:%M:%S')] [$level] $message\"\n}\n\nlog \"INFO\" \"Starting deployment\"\nlog \"ERROR\" \"Connection failed\"\n\n# Return values\nis_root() {\n    if [ \"$EUID\" -eq 0 ]; then\n        return 0             # 0 = success (true)\n    else\n        return 1             # non-zero = failure (false)\n    fi\n}\n\nif is_root; then\n    echo \"Running as root\"\nfi\n\n# === The FAANG production pattern: trap and cleanup ===\ncleanup() {\n    log \"INFO\" \"Cleaning up...\"\n    rm -f /tmp/deploy.$$\n    log \"INFO\" \"Cleanup complete\"\n}\ntrap cleanup EXIT          # cleanup runs on any exit (success, error, signal)\ntrap 'exit 1' INT TERM     # Handle Ctrl+C gracefully\n\n# FAANG standard deployment wrapper pattern:\ndeploy() {\n    local version=\"$1\"\n    log \"INFO\" \"Deploying version $version\"\n    \n    if ! ./preflight.sh; then\n        log \"ERROR\" \"Preflight failed\"\n        return 1\n    fi\n    \n    ./deploy.sh \"$version\"\n    log \"INFO\" \"Deploy complete\"\n}\n\n# Run it\ndeploy \"v2.3.1\" || exit 1"
    },
    {
      "title": "FAANG Interview Q&A",
      "description": "<strong>Q:</strong> 'Write a script that monitors a log file for ERROR patterns and sends an alert if more than 10 errors appear in 60 seconds.'<br><br><strong>A:</strong> '<code>tail -f /var/log/app.log | grep --line-buffered ERROR | head -10</code> — but for a real solution, use a while loop with timestamp checking and a counter. In production, you'd use a proper monitoring system (Prometheus, Datadog) — this script is for bootstrap scenarios.'<br><br><strong>Q:</strong> 'What does set -euo pipefail do, and why do you always use it in production scripts?'<br><br><strong>A:</strong> '-e: exit on first error. -u: error on unset variables (catches typos). -o pipefail: propogate failures through pipes. Without it, 'false | true' would succeed. These three flags turn shell scripts from fragile collections of commands into reliable programs.'<br><br><strong>Q:</strong> 'Write a script that backs up /var/lib/mysql to /backup, rotating the last 7 days.'<br><br><strong>A:</strong> See the code block for a complete backup rotation script.",
      "codeBlock": "#!/bin/bash\nset -euo pipefail\n\nBACKUP_DIR=\"/backup/mysql\"\nSOURCE=\"/var/lib/mysql\"\nRETENTION=7\n\nmkdir -p \"$BACKUP_DIR\"\n\n# Create timestamped backup\nTIMESTAMP=$(date '+%Y%m%d_%H%M%S')\nBACKUP_FILE=\"${BACKUP_DIR}/mysql_backup_${TIMESTAMP}.tar.gz\"\n\ntar -czf \"$BACKUP_FILE\" \"$SOURCE\"\necho \"Created: $BACKUP_FILE\"\n\n# Remove backups older than RETENTION days\nfind \"$BACKUP_DIR\" -name 'mysql_backup_*.tar.gz' -mtime +$RETENTION -delete\necho \"Cleaned up backups older than $RETENTION days\"\n\n# Verify backup integrity\ntar -tzf \"$BACKUP_FILE\" > /dev/null && echo \"Backup verified successfully\"\n\n# Report to monitoring\ncurl -s \"https://monitor.internal/report?metric=mysql.backup&value=1\" > /dev/null || true"
    },
    {
      "title": "References & Further Reading",
      "description": "<ul><li><a href=\"https://www.gnu.org/software/bash/manual/bash.html\"><b>Bash Reference Manual</b></a> — Official documentation</li><li><a href=\"https://google.github.io/styleguide/shellguide.html\"><b>Google Shell Style Guide</b></a> — FAANG standard for shell script style</li><li><a href=\"https://www.shellcheck.net/\"><b>ShellCheck</b></a> — Shell script static analysis (use this!)</li></ul>",
      "codeBlock": ""
    }
  ],
  "details": "<p class=\"mb-3\"><strong>FAANG Architect's Note:</strong> Shell scripts are the most common type of code I see deployed in production that has <strong>no tests</strong>. At FAANG, we enforce: (1) ShellCheck runs as part of CI, (2) scripts must have error handling (set -euo pipefail), (3) no script longer than 200 lines without justification. Longer scripts should be rewritten in Python or Go with proper testing.</p><p><strong>Interview Tip:</strong> When asked to write a shell script in an interview, always start with 'set -euo pipefail' and '#!/bin/bash'. These lines alone signal production experience. Also, remember that shell scripts are glue code — they call other programs, handle arguments, and manage flow. Keep them simple.</p>",
  "tags": ["FAANG", "Linux", "shell-scripting", "bash", "interview"]
}