{
  "id": "linux-git",
  "title": "Git Version Control",
  "category": "Linux",
  "subcategory": "Foundations",
  "language": "bash",
  "description": "Git is the version control system that underpins all modern software development. Created by Linus Torvalds for Linux kernel development, Git is now used by every FAANG company. Understanding Git deeply — not just add/commit/push — is essential for working in large engineering teams.",
  "codeBlock": "",
  "sections": [
    {
      "title": "Git Objects: Blobs, Trees, Commits",
      "description": "Git is fundamentally a <strong>content-addressable filesystem</strong> with a VCS UI on top. Every file version is stored as a blob, directories as trees, and snapshots as commits — all referenced by their SHA-1 hash. Understanding this model is the key to mastering Git.",
      "codeBlock": "# Git's object model:\n# blob  → file content (hash of content)\n# tree  → directory listing (maps names → blobs/trees)\n# commit → snapshot + metadata (tree hash, parent hash, author, message)\n\n# Explore the object database:\ngit init myrepo && cd myrepo\necho \"hello\" > file.txt\ngit add file.txt\ngit commit -m \"Initial commit\"\n\n# What just happened:\ngit cat-file -p HEAD\n# tree <tree-hash>\n# author Kallol ...\n# committer Kallol ...\n# Initial commit\n\ngit cat-file -p HEAD^{tree}\n# 100644 blob <blob-hash>    file.txt\n\ngit cat-file -p <blob-hash>\n# hello\n\n# Key insight: Git stores content, not files.\n# If you rename a file without changing content, the blob is reused.\n# If two files have the same content, they share a blob.\n# This is why git is space-efficient for text files."
    },
    {
      "title": "Branching & Merging Strategies",
      "description": "Branches are lightweight pointers to commits. Creating a branch is nearly instant — just a 41-byte file. FAANG teams use various branching strategies; understanding the tradeoffs between merge, rebase, and squash is an interview topic.",
      "codeBlock": "# Branches are just labels on commits\n# main → C3 ← feature → C4\n#              ↖         ↗\n#               merge commit\n\ngit branch              # List local branches\ngit branch feature      # Create branch from HEAD\ngit checkout feature    # Switch to branch\ngit checkout -b feature # Create and switch (one command)\ngit switch feature      # Modern alternative to checkout\n\n# === Merge ===\ngit checkout main\ngit merge feature       # Creates a merge commit\n# Pros: preserves history, safe\n# Cons: cluttered history with merge commits\n\n# === Rebase ===\ngit checkout feature\ngit rebase main         # Replay feature commits on top of main\ngit checkout main\ngit merge feature       # Fast-forward merge\n# Pros: linear history, clean\n# Cons: rewrites history (never rebase shared branches!)\n\n# === Squash ===\ngit merge --squash feature\ngit commit -m \"Add feature\"\n# Pros: single commit per feature\n# Cons: loses intermediate commits (debugging harder)\n\n# FAANG common workflows:\n# Google: squash-merge (single commit per CL)\n# Meta: rebase + merge (linear history)\n# Amazon: merge commits (preserve full history)"
    },
    {
      "title": "Interactive Rebase & History Rewriting",
      "description": "Interactive rebase lets you rewrite history before sharing. <strong>Never rewrite history on shared/public branches.</strong> For personal feature branches, it keeps the commit log clean and reviewable.",
      "codeBlock": "# Interactive rebase — reorder, squash, edit, drop commits\ngit rebase -i HEAD~5     # Rebase last 5 commits\n\n# In the editor, you'll see:\n# pick abc1234 First commit\n# pick def5678 Second commit\n# pick hij9010 Third commit\n#\n# Commands:\n# pick   = use commit as-is\n# reword = change commit message\n# edit   = stop to amend\n# squash = combine with previous commit\n# fixup  = squash but discard message\n# drop   = delete commit\n\n# Example: squash the last 3 commits into one:\ngit rebase -i HEAD~3\n# Change 'pick' to 'squash' for commits 2 and 3\n\n# Amend the last commit (fix message or add forgotten changes)\ngit add forgotten-file.txt\ngit commit --amend --no-edit   # Add to last commit without editing message\n\n# Undo the last commit but keep changes staged\ngit reset --soft HEAD~1\n\n# Undo the last commit and unstage changes\ngit reset HEAD~1               # --mixed (default)\n\n# Completely discard last commit and changes\ngit reset --hard HEAD~1        # DANGER: destructive!"
    },
    {
      "title": "Git in Production: The FAANG Workflow",
      "description": "At FAANG, Git workflows enforce code review, CI/CD, and deployment gates. Understanding these patterns helps you navigate large codebases and collaborate effectively.",
      "codeBlock": "# === Typical FAANG Feature Workflow ===\n\n# 1. Start from latest main\ngit checkout main\ngit pull --rebase          # Fetch + rebase local changes\n\n# 2. Create feature branch\ngit checkout -b feat/my-feature\n\n# 3. Make changes, commit often (incrementally)\ngit add -p                 # Interactive staging (hunk by hunk)\ngit commit -m \"feat: add user auth\"\n\n# 4. Keep branch updated with main\ngit fetch origin\ngit rebase origin/main     # Rebase instead of merge\n\n# 5. Final cleanup before push\ngit rebase -i origin/main  # Squash fixup commits\ngit log --oneline          # Verify clean history\n\n# 6. Push and create pull request\ngit push -u origin feat/my-feature\n# (Open PR in GitHub/GitLab/Bitbucket)\n\n# 7. Address review feedback\n# Make changes, then:\ngit add -p\ngit commit --amend --no-edit  # Keep single commit (if squashing)\n# or: just add more commits (if merging)\ngit push --force-with-lease   # Safe force-push after rebase\n\n# 8. Merge (via PR button — squash or merge commit)\n\n# === Cherry-pick for hotfixes ===\ngit checkout release/v2.0\ngit cherry-pick abc1234    # Pick a specific commit from main\n\n# === Bisect: find the commit that introduced a bug ===\ngit bisect start\ngit bisect bad             # Current commit is bad\ngit bisect good v1.0       # v1.0 is known good\n# Git checks out a midpoint commit — test it\ngit bisect good            # Or: git bisect bad\n# Repeat until the first bad commit is found\ngit bisect reset"
    },
    {
      "title": "Git Configuration & Productivity Tips",
      "description": "A well-configured Git setup saves hours. These are the configurations every FAANG engineer sets up on day one.",
      "codeBlock": "# Global configuration (set once)\ngit config --global user.name \"Kallol Chakraborty\"\ngit config --global user.email \"kallol@example.com\"\ngit config --global core.editor vim\ngit config --global init.defaultBranch main\n\n# Useful aliases\ngit config --global alias.lg \"log --oneline --graph --all --decorate\"\ngit config --global alias.last \"log -1 HEAD\"\ngit config --global alias.unstage \"reset HEAD --\"\ngit config --global alias.amend \"commit --amend --no-edit\"\ngit config --global alias.st \"status -sb\"\n\n# Better diffing\ngit config --global diff.algorithm patience\ngit config --global diff.context 5\n\n# .gitignore essentials for Linux projects:\n# *.log\n# *.swp\n# .DS_Store\n# build/\n# dist/\n# node_modules/\n# .env\n\n# === FAANG scenario: large monorepo ===\ngit clone --depth 1 <repo>          # Shallow clone (no history)\ngit clone --filter=blob:none <repo> # Partial clone (no blobs until needed)\ngit sparse-checkout set /path       # Checkout only specific directories\n\n# === Stashing ===\ngit stash                    # Save uncommitted changes\ngit stash -u                 # Include untracked files\ngit stash pop                # Restore and remove stash\ngit stash list               # List all stashes\ngit stash drop stash@{0}     # Delete a stash"
    },
    {
      "title": "FAANG Interview Q&A",
      "description": "<strong>Q:</strong> 'Explain the difference between git merge and git rebase. When would you use each?'<br><br><strong>A:</strong> 'Merge creates a merge commit that ties two branches together — it preserves history exactly as it happened. Rebase rewrites your feature branch commits on top of the target branch — it creates a linear history. Use merge for shared branches (main, release) to preserve the true sequence of events. Use rebase for personal feature branches to keep history clean before review.'<br><br><strong>Q:</strong> 'You accidentally committed to the wrong branch. How do you move the commit to the correct branch?'<br><br><strong>A:</strong> '<code>git log</code> to find the commit hash. <code>git checkout correct-branch</code>. <code>git cherry-pick <hash></code>. <code>git checkout wrong-branch</code>. <code>git reset --hard HEAD~1</code>. Or use <code>git rebase --onto</code> to move a range of commits.'<br><br><strong>Q:</strong> 'A junior engineer force-pushed to main and rewrote history. How do you recover?'<br><br><strong>A:</strong> '<code>git reflog</code> on the remote (if you have access) to find the original commit. <code>git reset --hard <original-commit></code>. <code>git push --force-with-lease</code> to restore. Enable branch protection on main to prevent this — main should require PRs and disallow force-push.'",
      "codeBlock": ""
    },
    {
      "title": "References & Further Reading",
      "description": "<ul><li><a href=\"https://git-scm.com/book/en/v2\"><b>Pro Git</b></a> — The free online Git book (Scott Chacon & Ben Straub)</li><li><a href=\"https://www.atlassian.com/git/tutorials\"><b>Atlassian Git Tutorials</b></a> — Visual explanations of Git concepts</li><li><a href=\"https://ohshitgit.com/\"><b>Oh Shit, Git!</b></a> — Practical recoveries for common Git mistakes</li></ul>",
      "codeBlock": ""
    }
  ],
  "details": "<p class=\"mb-3\"><strong>FAANG Architect's Note:</strong> Git was created to solve a specific problem: 20,000+ developers contributing to the Linux kernel simultaneously. The FAANG monorepo models (Google's Piper, Meta's Mercurial-based repo) are more sophisticated, but Git skills translate directly. If you understand Git's object model deeply, you'll understand any VCS.</p><p><strong>Interview Tip:</strong> When asked Git questions, show depth by referencing the underlying object model (blobs, trees, commits). Candidates who only know 'add, commit, push' don't demonstrate senior-level understanding. Know how to recover from mistakes — that's what separates experienced engineers.</p>",
  "tags": ["FAANG", "Linux", "git", "version-control", "interview"]
}