{
  "id": "linux-filesystem",
  "title": "Filesystem Hierarchy Standard",
  "category": "Linux",
  "subcategory": "Foundations",
  "language": "bash",
  "description": "The Filesystem Hierarchy Standard (FHS) defines the directory structure on Linux systems. Knowing where things live — and why — is table-stakes for every FAANG infrastructure interview. When an interviewer says 'your service is logging to /var/log and filling up the disk,' you need to know where everything is.",
  "codeBlock": "",
  "sections": [
    {
      "title": "The Root Filesystem (/)",
      "description": "Everything on Linux starts at <code>/</code>. The root filesystem is the top of the single unified directory tree — there are no drive letters (C:, D:). Every storage device is <strong>mounted</strong> at a directory under <code>/</code>. This unified namespace is one of Unix's most important innovations.",
      "codeBlock": "# Essential root directories at a glance\n/bin      # Essential user command binaries (available in single-user mode)\n/sbin     # System administration binaries (e.g., fdisk, mkfs, iptables)\n/etc      # Host-specific system configuration (the nerve center)\n/var      # Variable data — logs, databases, spool files\n/tmp      # Temporary files (cleared on boot, world-writable)\n/usr      # Secondary hierarchy — user utilities and applications\n/opt      # Add-on application software packages\n/proc     # Virtual filesystem for process and kernel information\n/sys      # Virtual filesystem for kernel objects and device info\n/dev      # Device files (every hardware device is a file)\n/run      # Runtime variable data (PID files, sockets — tmpfs since boot)\n/home     # User home directories\n/root     # Root user's home directory\n\n# A FAANG Architect's quick map:\n# /etc  → Configuration\n# /var  → Data (logs, DBs)\n# /proc → Kernel/process introspection\n# /dev  → Hardware as files\n# /tmp  → Scratch space (watch for security!)"
    },
    {
      "title": "/etc — The Configuration Nerve Center",
      "description": "<code>/etc</code> holds all host-specific configuration. If something isn't working, you start here. In FAANG production environments, /etc is typically managed by configuration management (Ansible, Puppet, Chef) and changes are reviewed through code review.",
      "codeBlock": "ls /etc\n# Key files every engineer should know:\n/etc/passwd       # User account info (login shells, UIDs)\n/etc/shadow       # Encrypted password hashes (root-readable only)\n/etc/group        # Group definitions\n/etc/sudoers      # Sudo privileges (edit with visudo!)\n/etc/hosts        # Static hostname-to-IP mapping\n/etc/resolv.conf  # DNS resolver configuration\n/etc/hostname     # System hostname\n/etc/fstab        # Filesystem mount table (auto-mount on boot)\n/etc/crontab      # System-wide cron schedule\n/etc/ssh/sshd_config   # SSH server configuration\n/etc/nginx/nginx.conf  # Nginx configuration (if installed)\n/etc/systemd/    # Systemd unit files and system configuration\n\n# Tip: /etc is typically on the root filesystem\n# Don't let logs fill up / — it can make the system unbootable"
    },
    {
      "title": "/proc & /sys — Virtual Filesystems for Kernel Introspection",
      "description": "<code>/proc</code> and <code>/sys</code> are virtual filesystems — they don't exist on disk. Reading files in /proc triggers kernel code that returns real-time data. Writing to some files changes kernel parameters. These are your window into the running kernel. Every FAANG SRE knows /proc intimately.",
      "codeBlock": "# === /proc — Process and System Information ===\ncat /proc/cpuinfo      # CPU details (model, cores, flags, cache)\ncat /proc/meminfo      # Memory: total, free, buffers, cache, swap\ncat /proc/uptime       # System uptime in seconds\ncat /proc/version      # Linux kernel version string\ncat /proc/loadavg      # System load averages (1, 5, 15 min)\ncat /proc/1/cmdline    # Command line of PID 1 (init/systemd)\ncat /proc/self/status  # Current process's status (Name, Pid, VmRSS, ...)\nls /proc/<PID>/fd/     # Open file descriptors for a process\ncat /proc/<PID>/maps   # Memory-mapped regions of a process\n\n# === /proc/sys — Kernel Tunables (sysctl interface) ===\ncat /proc/sys/net/ipv4/ip_forward        # Is IP forwarding enabled?\ncat /proc/sys/net/ipv4/tcp_tw_reuse      # TCP TIME_WAIT reuse\ncat /proc/sys/vm/swappiness              # Swap tendency (0-100)\ncat /proc/sys/vm/dirty_ratio             # Max dirty pages %% before writeback\n\n# === /sys — Device and Driver Model ===\nls /sys/class/block/    # All block devices (sda, nvme0n1, etc.)\nls /sys/class/net/      # All network interfaces (eth0, lo, wlan0)\nls /sys/devices/system/cpu/  # Per-CPU information\n\n# FAANG insight: When debugging production issues,\n# /proc/<PID>/ is your first stop for process-level diagnostics"
    },
    {
      "title": "/var — Variable Data (Logs, Databases, Spool)",
      "description": "<code>/var</code> holds data that changes in size and content during normal operation. This is where logs go, and disk space monitoring of /var is a standard part of any SRE runbook. <strong>Common FAANG interview scenario:</strong> 'Disk is full, find and clean up /var/log without restarting services.'",
      "codeBlock": "ls /var\n/var/log          # System and application logs (journalctl, syslog, nginx)\n/var/lib          # Persistent state data (databases: mysql, postgres, docker)\n/var/cache        # Application cache data (apt, yum, pip)\n/var/spool        # Print and mail spools\n/var/tmp          # Temporary files preserved across reboots\n/var/opt          # Variable data for /opt packages\n\n# === FAANG Scenario: Log Rotation ===\n# Logrotate manages log rotation. Config at /etc/logrotate.conf\n# Watch for: services that log too verbosely, filling /var\n\n# Check disk usage by directory:\ndu -sh /var/log/* 2>/dev/null | sort -rh | head -10\n\n# Safely truncate a log without restarting:\n# (The file descriptor stays valid even after truncation)\n> /var/log/myapp/access.log          # Truncate in place\n# OR:\ntruncate -s 0 /var/log/myapp/access.log"
    },
    {
      "title": "FAANG Interview Q&A",
      "description": "<strong>Q:</strong> 'Your production server is running out of disk space on /. Walk through your debugging process.'<br><br><strong>A:</strong> 'First, <code>df -h</code> to see which filesystem is full. Then <code>du -sh /* | sort -rh</code> to find the largest top-level directory. Common culprits: /var/log (unrotated logs), /tmp (temp files from batch jobs), Docker overlay storage in /var/lib/docker. For /var/log, check logrotate config. For Docker, prune unused images and containers. In a FAANG environment, we'd have monitoring alerting on disk usage at 80%, 90%, 95% — and automated cleanup via cron or systemd timers.'<br><br><strong>Q:</strong> 'What's the difference between /proc and /sys?'<br><br><strong>A:</strong> '/proc was originally process information — the original design was one directory per process (/proc/PID). It evolved to include system info (/proc/cpuinfo, /proc/meminfo, /proc/sys). /sys (sysfs) is newer — it exports the kernel's device and driver model in a structured way. /sys/class organizes by type (block, net, input), /sys/devices by topology. Both are virtual filesystems backed by kernel callbacks. Use /proc for process data and kernel tunables; use /sys for hardware and driver information.'<br><br><strong>Q:</strong> 'Why does Linux use forward slashes (/) instead of backslashes (\\\\)?'<br><br><strong>A:</strong> 'Historical compatibility with the MULTICS/Unix design choice. Forward slashes were chosen because they didn't conflict with the shell's escape character (backslash). Every major OS except Windows uses forward slashes.'",
      "codeBlock": ""
    },
    {
      "title": "References & Further Reading",
      "description": "<ul><li><a href=\"https://refspecs.linuxfoundation.org/FHS_3.0/fhs-3.0.pdf\"><b>Filesystem Hierarchy Standard 3.0</b></a> — The official FHS specification</li><li><a href=\"https://man7.org/linux/man-pages/man7/hier.7.html\"><b>man hier</b></a> — Description of the filesystem hierarchy on your system</li><li><a href=\"https://www.tldp.org/LDP/Linux-Filesystem-Hierarchy/html/\"><b>Linux Filesystem Hierarchy</b></a> — TLDP comprehensive guide</li></ul>",
      "codeBlock": ""
    }
  ],
  "details": "<p class=\"mb-3\"><strong>FAANG Architect's Note:</strong> The Unix filesystem hierarchy isn't arbitrary — it reflects the system's engineering philosophy: <strong>everything is a file</strong>. Devices (/dev), processes (/proc), kernel parameters (/proc/sys) — all accessible with standard file APIs (open, read, write, close). This uniform interface is why you can use the same tools (grep, awk, cat) on hardware status and text files alike.</p><p class=\"mb-3\"><strong>Production Tip:</strong> In containerized environments (Docker/Kubernetes), some of these directories behave differently. /proc and /sys are typically mounted from the host kernel (read-only for security). Never assume a container has access to all /proc entries — security_context settings may restrict them.</p><p><strong>Interview Tip:</strong> When asked 'how do you debug X on Linux,' always start with the filesystem location of the relevant data. Interviewers test your muscle memory — can you navigate to the right /proc file or /var/log without googling?</p>",
  "tags": ["FAANG", "Linux", "filesystem", "interview"]
}