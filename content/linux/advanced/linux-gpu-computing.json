{
  "id": "linux-gpu-computing",
  "title": "GPU Computing on Linux",
  "category": "Linux",
  "subcategory": "Advanced",
  "language": "bash",
  "description": "GPU computing on Linux is the backbone of ML/AI at FAANG. Understanding the GPU driver stack — from kernel modules to CUDA runtime to container integration — is essential for ML engineers, infrastructure engineers, and anyone deploying GPU workloads at scale. The Linux ecosystem for GPUs spans NVIDIA (dominant), AMD (ROCm), and Intel (oneAPI).",
  "codeBlock": "",
  "sections": [
    {
      "title": "GPU Stack: From Kernel to Application",
      "description": "The GPU software stack has multiple layers: <strong>kernel drivers</strong> (nvidia.ko, nvidia-uvm.ko), <strong>user-space libraries</strong> (libcuda.so, libcudart.so), <strong>compute frameworks</strong> (CUDA, cuDNN, TensorRT), and <strong>orchestration</strong> (NVIDIA container toolkit, MIG). At FAANG, this stack is baked into custom AMIs and container images — engineers don't install it manually, but understanding it is essential for debugging.",
      "codeBlock": "# === Check GPU hardware ===\nlspci | grep -i nvidia               # List NVIDIA GPUs\nlspci -v -s 01:00.0                  # Detailed GPU info\n\n# === NVIDIA driver modules ===\nlsmod | grep nvidia                  # nvidia, nvidia_uvm, nvidia_drm, nvidia_modeset\n# nvidia.ko       — Core driver\n# nvidia-uvm.ko   — Unified Virtual Memory (CUDA unified memory)\n# nvidia-drm.ko   — Direct Rendering Manager (display)\n# nvidia-modeset.ko — Mode setting (display)\n\n# === nvidia-smi (the single most important GPU command) ===\nnvidia-smi                           # Summary: GPUs, processes, memory, temp\nnvidia-smi -L                        # List all GPUs with UUIDs\nnvidia-smi --query-gpu=index,name,temperature.gpu,utilization.gpu,memory.used,memory.total --format=csv\nnvidia-smi pmon -c 1                 # Per-process GPU usage\nnvidia-smi dmon -c 1                 # Per-device monitoring\nnvidia-smi topo -m                   # GPU topology (NVLink, PCIe)\n\n# === GPU health ===\nnvidia-smi -q -d TEMPERATURE         # Temperature details\nnvidia-smi -q -d POWER               # Power draw and limits\nnvidia-smi -q -d CLOCK               # Clock speeds (graphics, memory, SM)\n\n# === ECC (Error Correcting Code) ===\nnvidia-smi -q -d ECC                 # ECC errors (memory bit flips)\n# ECC is ON by default on datacenter GPUs (A100, H100, V100)\n# ECC OFF on consumer GPUs (RTX 4090) — trades reliability for speed\n\n# === GPU persistence daemon ===\nsudo nvidia-persistenced --verbose    # Keep GPU initialized between processes\n# Without it: first CUDA call on each GPU takes ~1 second (driver init)\n# With it: driver stays loaded, zero startup latency\n\n# === Reset GPU (if hung) ===\nsudo nvidia-smi --gpu-reset 0         # Reset GPU 0 (only on Tesla/Quadro)"
    },
    {
      "title": "GPU Performance & Profiling",
      "description": "GPU profiling identifies bottlenecks: is your kernel compute-bound (SM utilization high), memory-bound (memory bandwidth high), or latency-bound (kernel launch overhead)? <strong>At FAANG, every ML training run is profiled — it's part of the CI pipeline for model training.</strong> Tools: nvidia-smi, NSight Systems, NSight Compute, and PyTorch profiler.",
      "codeBlock": "# === Real-time GPU monitoring ===\nwatch -n 0.5 nvidia-smi              # Refresh every 500ms\n\n# === GPU utilization patterns ===\n# 0-30%:   Underutilized (small batch size, CPU bottleneck, data loading issue)\n# 30-70%:  Moderate (typical for distributed training with comm overhead)\n# 70-95%:  Good utilization (efficient training)\n# 95-100%: Saturated (GPU is the bottleneck — ideal for throughput)\n\n# === Key metrics to watch ===\n# Volatile GPU-Util — SM utilization (not a perfect metric, hides underutilization)\n# Memory-Usage — How much GPU memory is used\n# Temperature — Throttling starts at ~85°C for most GPUs\n# Power Draw — If below TDP, GPU may be power-limited or underutilized\n\n# === CUDA-MEMCHECK (memory errors in GPU kernels) ===\ncuda-memcheck ./my_cuda_program       # Check for out-of-bounds, misaligned access\n\n# === NSight Systems (system-level profiling) ===\n# Profile CPU + GPU interactions:\nnsys profile --stats=true -o profile python train.py\n# Shows: CPU kernel time, GPU kernel time, CUDA API calls, memory operations\n\n# === NSight Compute (kernel-level profiling) ===\nncu --target-processes all python train.py  # Profile individual CUDA kernels\n\n# === PyTorch profiler ===\n# python -m torch.utils.bottleneck train.py\n# Or:\n# with torch.profiler.profile(activities=[...]) as prof:\n#     run_training()\n# print(prof.key_averages().table(sort_by=\"cuda_time_total\"))\n\n# === NCCL (NVIDIA Collective Communications Library) ===\n# Used for multi-GPU and multi-node communication\n# Debug: NCCL_DEBUG=INFO, NCCL_DEBUG_SUBSYS=ALL\n# Performance: NCCL_ALGO=Ring, NCCL_PROTO=Simple\nnccl-tests/build/all_reduce_perf -b 128M -e 8G -f 2 -g 8  # Benchmark NCCL\n\n# === CUDA graphs (reduce kernel launch overhead) ===\n# Captures a sequence of GPU operations and replays them\n# Reduces CPU launch overhead — critical for small-batch inference\n# Enabled by default in PyTorch 2.x with torch.compile"
    },
    {
      "title": "GPU Multi-Tenancy: MIG, Time-Slicing & MPS",
      "description": "Modern datacenter GPUs support sharing a physical GPU across multiple workloads. <strong>MIG (Multi-Instance GPU)</strong> partitions A100/H100 into up to 7 isolated instances with dedicated memory and compute. <strong>Time-slicing</strong> shares GPU time (no memory isolation). <strong>MPS</strong> shares CUDA context across processes. At FAANG, MIG is used for cost-efficient inference serving — partitioning a single A100 into 3x20GB instances for 3 models.",
      "codeBlock": "# === MIG (Multi-Instance GPU) — A100/H100 only ===\n# List MIG capabilities:\nnvidia-smi mig -i 0                   # Check MIG support for GPU 0\n\n# Enable MIG on GPU 0:\nsudo nvidia-smi -mig 1 -i 0\nsudo nvidia-smi mig -cgi 19,19,19,19  # Create 4 compute instances, 20GB each\n# 19 = 'MIG 3g.20gb' profile (3 GPU slices, 20GB) on A100\n# Profiles: 1g.10gb, 2g.20gb, 3g.40gb, 4g.40gb, 7g.80gb\n\n# List MIG instances:\nnvidia-smi mig -lgi\n\n# Assign MIG device to container:\ndocker run --gpus '\"device=MIG-<UUID>\"' nvidia/cuda:12.0-runtime\n\n# === Time-slicing (Kubernetes) ===\n# GPU sharing without memory isolation\n# Each pod gets GPU time but can use all GPU memory\n# Best for: burstable inference workloads\n# Configure via NVIDIA device plugin with 'time-slicing' config\n\n# === MPS (Multi-Process Service) ===\n# Enables CUDA context sharing across processes\n# Better utilization for many small inference requests\nexport CUDA_MPS_PIPE_DIRECTORY=/tmp/mps_pipe\nexport CUDA_MPS_LOG_DIRECTORY=/tmp/mps_log\nnvidia-cuda-mps-control -d           # Start MPS daemon\n# All CUDA processes now share a single CUDA context\n# echo \"quit\" | nvidia-cuda-mps-control  # Stop MPS\n\n# === GPU memory bandwidth contention ===\n# Multiple processes sharing GPU memory bandwidth can conflict\n# MIG isolates memory bandwidth; time-slicing does not\n# At FAANG: inference workloads use MIG; training gets full GPU\n\n# === GPU oversubscription (CUDA Unified Memory) ===\n# GPU can access more memory than physical VRAM via page faults\n# Slow! Only use for prototyping\n# Production: allocate within GPU memory limits"
    },
    {
      "title": "NVIDIA Container Toolkit & Kubernetes GPU",
      "description": "GPU access in containers requires the <strong>NVIDIA Container Toolkit</strong> (nvidia-docker2 / nvidia-container-toolkit). It mounts GPU drivers and CUDA libraries into containers. At FAANG, GPU pods are scheduled via the <strong>NVIDIA Kubernetes Device Plugin</strong>, which advertises GPU count per node and handles MIG partitioning.",
      "codeBlock": "# === NVIDIA Container Toolkit ===\n# Install:\nsudo apt install nvidia-container-toolkit\nsudo nvidia-ctk runtime configure --runtime=docker\nsudo systemctl restart docker\n\n# Test:\ndocker run --gpus all nvidia/cuda:12.0-runtime nvidia-smi\n\n# === How it works ===\n# The toolkit mounts:\n#   /usr/lib/x86_64-linux-gnu/libcuda.so  — CUDA driver library\n#   /usr/lib/x86_64-linux-gnu/libnvidia-*  — NVIDIA libraries\n#   /proc/driver/nvidia/                   — Driver information\n#   /dev/nvidia*                           — GPU device files\n# Without toolkit: container can't access /dev/nvidia0\n\n# === Kubernetes GPU scheduling ===\n# NVIDIA device plugin registers GPU resources\n# kubectl describe node | grep nvidia.com/gpu\n# Pod requests GPU:\n#   resources:\n#     limits:\n#       nvidia.com/gpu: 1\n#\n# Device plugin also handles MIG profiles:\n#   nvidia.com/mig-1g.10gb: 2\n\n# === CUDA version compatibility ===\n# nvidia-smi shows CUDA Version (driver API version)\n# Container can use any CUDA version <= driver version\n# Driver: 545.23.08 = supports CUDA 12.3 and older\n# Container can run: CUDA 12.3, 12.2, 12.1, ..., 11.x, 10.x, etc.\n# This is why FAANG uses: driver on host, CUDA toolkit in container\n\n# === GPU topology-aware scheduling ===\n# NVLink-connected GPUs communicate faster than PCIe\n# nvidia-smi topo -m shows connectivity\n# Kubernetes: use 'nvidia.com/gpu.nvlink' label for topology-aware scheduling\n\n# === GPU monitoring in containers ===\n# Inside container:\ndcgmi group -c all                     # Create DCGM group\ndcgmi stats -g 1 -s true               # Enable stats collection\ndcgmi dmon -e 100,101,102,200,201      # Key metrics: GPU util, mem util, temp, power, PCIe\n\n# Prometheus + DCGM Exporter:\n# https://github.com/NVIDIA/dcgm-exporter\n# Exposes: DCGM_FI_DEV_GPU_UTIL, DCGM_FI_DEV_MEM_COPY_UTIL, etc."
    },
    {
      "title": "FAANG Interview Q&A",
      "description": "<strong>Q:</strong> 'Your training job is running at 30% GPU utilization. What do you check?'<br><br><strong>A:</strong> 'Check nvidia-smi to confirm utilization. Then identify the bottleneck: is it data loading (CPU/IO bound), compute (kernel inefficient), or communication (NCCL sync). Use nvidia-smi dmon for per-GPU metrics, NSight Systems to profile the full pipeline. Common causes: (1) Data loader bottleneck — increase num_workers, use NVMe storage, enable pinned memory. (2) Small batch size — GPU finishes compute quickly, waits for next batch. (3) Gradient accumulation steps are inefficient. (4) Model has many small kernels with launch overhead — use torch.compile or CUDA graphs. (5) AllReduce communication overhead in multi-GPU — check NCCL settings and network bandwidth. At FAANG, we have a standard 'GPU health check' script that profiles the full stack before every training run.'<br><br><strong>Q:</strong> 'How do you choose MIG vs time-slicing vs full GPU for production inference?'<br><br><strong>A:</strong> 'MIG provides hard isolation — guaranteed memory and compute, no interference. Use MIG when you need QoS guarantees (e.g., a latency-sensitive model sharing GPU with a throughput-oriented model). Time-slicing provides compute sharing but no memory isolation — a memory leak in one container can crash others. Use time-slicing for burstable workloads where occasional interference is acceptable. Full GPU is for large models that need all memory (LLMs, large Vision models) or for training. At FAANG, we use MIG for production inference serving and time-slicing for development/staging environments where cost efficiency matters more than isolation.'<br><br><strong>Q:</strong> 'An ML engineer says 'CUDA out of memory' on an A100 with 80GB, but the model should fit in 40GB. What do you check?'<br><br><strong>A:</strong> 'Several possibilities: (1) Memory fragmentation — allocate largest tensors first, use memory pools. (2) Intermediate memory — activation memory can exceed parameter memory for large batch sizes. Reduce batch size or use gradient checkpointing. (3) Memory leak — tensors not freed between epochs (Python references holding GPU memory). Use torch.cuda.empty_cache() and check for reference cycles. (4) The engineer is not using MIG and another process is using the remaining memory — check nvidia-smi for other processes on that GPU. (5) NCCL buffers — NCCL allocates scratch memory for AllReduce. Set NCCL_BUFFSIZE or use environment variables. At FAANG, we run nvidia-smi and then do a memory allocation test to find the exact available memory before launching training.'",
      "codeBlock": ""
    },
    {
      "title": "References & Further Reading",
      "description": "<ul><li><a href=\"https://docs.nvidia.com/deploy/nvidia-smi/\"><b>nvidia-smi Documentation</b></a> — Complete reference for GPU monitoring and management</li><li><a href=\"https://docs.nvidia.com/datacenter/cloud-native/container-toolkit/overview.html\"><b>NVIDIA Container Toolkit</b></a> — GPU access in Docker and Kubernetes</li><li><a href=\"https://docs.nvidia.com/deeplearning/nvidia-user-guide/index.html\"><b>NVIDIA Deep Learning Documentation</b></a> — CUDA, cuDNN, TensorRT, and NGC catalog</li></ul>",
      "codeBlock": ""
    }
  ],
  "details": "<p class=\"mb-3\"><strong>FAANG Architect's Note:</strong> GPU computing is where Linux infrastructure meets ML engineering. The most common failure mode I see: engineers treating GPUs as 'magic accelerators' rather than understanding the driver stack. When a training job is slow, the instinct is to blame the GPU — but 90% of the time, the bottleneck is elsewhere: data loading (CPU/IO), framework overhead (Python GIL, tensor copies), or network communication (NCCL sync). The engineers who stand out are those who can profile the entire pipeline: they watch nvidia-smi, strace the data loader, NCCL debug the communication, and NSight-profile the kernels. Build this skill systematically — each training job is an opportunity to understand the stack better.</p><p><strong>Production Tip:</strong> Always set CUDA_LAUNCH_BLOCKING=1 when debugging GPU hangs. This forces synchronous kernel launches — instead of queuing all kernels and then hanging, CUDA executes them one at a time, and you'll see exactly where it hangs. In production, remove this (synchronous launches kill performance). Also: never use torch.cuda.manual_seed_all() in production unless you need deterministic results — it slows down initial kernel launches on some GPU architectures.</p>",
  "tags": ["FAANG", "Linux", "GPU", "CUDA", "NVIDIA", "MIG", "machine learning", "profiling"]
}
