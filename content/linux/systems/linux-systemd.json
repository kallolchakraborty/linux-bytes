{
  "id": "linux-systemd",
  "title": "systemd: The Modern Init System",
  "category": "Linux",
  "subcategory": "Systems & Operations",
  "language": "bash",
  "description": "systemd is the init system and service manager for virtually every modern Linux distribution. It replaced SysV init and handles service startup, dependency management, logging, timers, socket activation, and resource control. At FAANG, understanding systemd is essential for writing production service units, debugging boot issues, and building container runtimes.",
  "codeBlock": "",
  "sections": [
    {
      "title": "Core Concepts: Units & Targets",
      "description": "systemd manages <strong>units</strong>. There are several types: <strong>service</strong> (daemons), <strong>socket</strong> (socket activation), <strong>timer</strong> (scheduled tasks, replacing cron), <strong>mount</strong> (filesystem mounts), <strong>target</strong> (group of units, replacing runlevels). Units are defined in <code>.unit</code> files located in <code>/usr/lib/systemd/system/</code> (distro) and <code>/etc/systemd/system/</code (local overrides).",
      "codeBlock": "# === Basic systemctl commands ===\nsystemctl status nginx             # Status + recent logs\nsystemctl start nginx              # Start immediately\nsystemctl stop nginx               # Stop\nsystemctl restart nginx            # Restart (stop + start)\nsystemctl reload nginx             # Send SIGHUP (reload config)\nsystemctl enable nginx             # Start on boot (creates symlinks)\nsystemctl disable nginx            # Remove boot symlinks\nsystemctl daemon-reload            # Reload unit files after edits\nsystemctl is-active nginx          # Check if running (exit code 0/1)\nsystemctl is-enabled nginx         # Check if enabled\n\n# === List units ===\nsystemctl list-units               # All active units\nsystemctl list-units --all         # All units (including inactive)\nsystemctl list-units --type=service  # Only services\nsystemctl list-unit-files          # All unit files + enable state\nsystemctl list-dependencies nginx  # Show dependency tree\n\n# === Targets (runlevels) ===\nsystemctl list-units --type=target  # List targets\nsystemctl get-default               # Default boot target\nsystemctl set-default multi-user.target  # Boot to multi-user (no GUI)\nsystemctl isolate emergency.target      # Switch to emergency mode\n\n# === Legacy runlevel mapping ===\n# runlevel 0 → poweroff.target\n# runlevel 1 → rescue.target\n# runlevel 3 → multi-user.target\n# runlevel 5 → graphical.target\n# runlevel 6 → reboot.target"
    },
    {
      "title": "Writing a Service Unit File",
      "description": "A <code>.service</code> unit defines how to start, stop, and manage a daemon. <strong>The [Unit] section describes dependencies, [Service] defines execution, and [Install] controls enable/disable behavior.</strong> At FAANG, every internally developed service ships with a production-grade systemd unit file that includes resource limits, health checks, and graceful shutdown settings.",
      "codeBlock": "# Example: /etc/systemd/system/myapp.service\n[Unit]\nDescription=My Production Service\nAfter=network-online.target\nWants=network-online.target\nRequires=postgresql.service\nAfter=postgresql.service\n\n[Service]\nType=simple                    # Default: process doesn't fork\nUser=myapp\nGroup=myapp\nWorkingDirectory=/opt/myapp\nExecStart=/usr/bin/myapp --config /etc/myapp/config.yaml\nExecReload=/bin/kill -HUP $MAINPID\nExecStop=/bin/kill -TERM $MAINPID\nRestart=always                 # Restart on crash\nRestartSec=10                  # Wait 10s before restart\nTimeoutStartSec=30             # Max time to start\nTimeoutStopSec=30              # Max time to stop gracefully\n\n# Resource limits\nLimitNOFILE=65536\nLimitNPROC=4096\nMemoryMax=512M\nCPUQuota=50%\n\n# Security\nProtectSystem=strict\nProtectHome=true\nPrivateTmp=true\nNoNewPrivileges=true\nCapabilityBoundingSet=CAP_NET_BIND_SERVICE\n\n[Install]\nWantedBy=multi-user.target\n\n# === Type variants ===\n# Type=simple       — Main process is ExecStart (default)\n# Type=forking      — Process forks (daemonizes); notify when parent exits\n# Type=oneshot      — Runs once (for setup/scripts); RemainAfterExit=yes\n# Type=notify       — Process calls sd_notify() when ready\n# Type=dbus         — Process registers on D-Bus; wait for bus name\n\n# === Environment ===\n# Set environment variables:\n[Service]\nEnvironment=MYAPP_ENV=production\nEnvironment=JAVA_HOME=/usr/lib/jvm/java-11-openjdk\nEnvironmentFile=-/etc/default/myapp     # File with KEY=VAL pairs\n\n# === Override without editing original ===\nsudo systemctl edit nginx           # Creates /etc/systemd/system/nginx.service.d/override.conf\nsudo systemctl cat nginx            # Show full unit with overrides applied"
    },
    {
      "title": "journalctl: The systemd Logging System",
      "description": "systemd replaced syslog with <strong>journald</strong>, a binary logging system with structured metadata (priority, facility, PID, UID, boot ID). <code>journalctl</code> queries the journal. <strong>In production debugging, journalctl -xe is often the first command run during an incident.</strong>",
      "codeBlock": "# === Basic journalctl usage ===\njournalctl                          # All logs (pager)\njournalctl -n 50                    # Last 50 lines\njournalctl -f                        # Follow (like tail -f)\njournalctl -u nginx                 # Logs for a specific unit\njournalctl -u nginx -u postgresql   # Multiple units\n\n# === Time-based filtering ===\njournalctl --since \"5 min ago\"\njournalctl --since today\njournalctl --since \"2024-01-01\" --until \"2024-01-02\"\njournalctl --since yesterday -u nginx\n\n# === Priority levels ===\njournalctl -p err                    # Only errors and above\njournalctl -p warning                # Warnings and above\n# Levels: emerg(0), alert(1), crit(2), err(3), warning(4), notice(5), info(6), debug(7)\n\n# === Output formats ===\njournalctl -o verbose                # All structured fields\njournalctl -o json                   # JSON output (for jq / log aggregation)\njournalctl -o json-pretty\n\n# === Disk usage ===\njournalctl --disk-usage\nsudo journalctl --vacuum-size=500M   # Keep only 500MB of logs\nsudo journalctl --vacuum-time=7d     # Keep only 7 days\n\n# === Boot-specific ===\njournalctl -b -1                     # Previous boot (useful after crash)\njournalctl -b -2                     # Two boots ago\njournalctl --list-boots              # List boot numbers + IDs\n\n# === Log a test message ===\nlogger \"Test log message\"            # Write to systemd journal\nlogger -p user.err \"Custom error\"    # With priority\n\n# === Config: /etc/systemd/journald.conf ===\n# SystemMaxUse=1G\n# Compress=yes\n# ForwardToSyslog=no"
    },
    {
      "title": "Timers (Replacing Cron) & Socket Activation",
      "description": "systemd timers replace cron with fine-grained scheduling, dependency support, and logging. <strong>Socket activation allows services to start on demand when a connection arrives — a lazy-start pattern used by many system services.</strong>",
      "codeBlock": "# === Timer example ===\n# /etc/systemd/system/backup.service\n[Service]\nType=oneshot\nExecStart=/usr/local/bin/backup.sh\n\n# /etc/systemd/system/backup.timer\n[Unit]\nDescription=Daily Backup\nRequires=backup.service\n\n[Timer]\nOnCalendar=daily\nPersistent=true            # If missed (e.g., during downtime), run immediately\nRandomizedDelaySec=3600    # Random delay within 1 hour (avoid thundering herd)\n\n[Install]\nWantedBy=timers.target\n\n# === Manage timers ===\nsystemctl enable --now backup.timer\nsystemctl list-timers\n# NEXT                        LEFT          LAST                        PASSED    UNIT\n# Mon 2024-01-15 03:00:00    14h left      Sun 2024-01-14 03:00:01   9h ago    backup.timer\n\n# === OnCalendar patterns ===\n# daily              → Every day at 00:00:00\n# hourly             → Every hour at :00\n# Mon *-*-* 09:00:00 → Every Monday at 9 AM\n# *-*-01 00:00:00    → First of every month\n# *:0/15             → Every 15 minutes\n\n# === Socket activation ===\n# Listen on a port; start service on first connection\n# /etc/systemd/system/echo.socket\n[Socket]\nListenStream=2000\n\n[Install]\nWantedBy=sockets.target\n\n# /etc/systemd/system/echo@.service\n[Service]\nExecStart=/usr/local/bin/echo-server\nStandardInput=socket\n\n# === Benefits of socket activation ===\n# 1. Lazy-start: service consumes resources only when needed\n# 2. Resilience: if service crashes, socket survives, next request restarts it\n# 3. Parallel boot: services can start while socket is already listening"
    },
    {
      "title": "System Analysis & Debugging",
      "description": "systemd provides powerful tools for analyzing boot performance, system state, and unit interactions. <strong>At FAANG, these tools are used in periodic 'boot health' checks and capacity planning reviews.</strong>",
      "codeBlock": "# === Boot analysis ===\nsystemd-analyze                    # Total boot time\nsystemd-analyze blame              # Time taken by each unit (sorted)\nsystemd-analyze critical-chain     # Critical chain of slowest dependencies\nsystemd-analyze plot > boot.svg    # Generate a visualization\n\n# === Unit dependency graph ===\nsystemctl list-dependencies nginx\nsystemd-analyze dot nginx | dot -Tsvg > nginx-deps.svg  # Visual graph\n\n# === Failed units ===\nsystemctl --failed                 # List failed units\n\n# === Check if systemd is running ===\nps -p 1 -o comm=                   # Should print 'systemd'\n\n# === Emergency / Recovery ===\nsystemctl rescue                   # Rescue mode (single-user, minimal services)\nsystemctl emergency                # Emergency shell (PID 1 only, root filesystem ro)\n\n# === Common issues ===\n# 1. 'Unit not found' — systemctl daemon-reload\n# 2. 'Dependency failed' — check required units (systemctl list-dependencies)\n# 3. 'Start request repeated too quickly' — RestartSec is too short\n# 4. 'Failed to connect to bus' — systemd is not PID 1 (e.g., inside container without systemd)"
    },
    {
      "title": "FAANG Interview Q&A",
      "description": "<strong>Q:</strong> 'How does systemd start services in parallel during boot?'<br><br><strong>A:</strong> 'systemd parses all unit files and builds a dependency graph. Units without inter-dependencies start in parallel using socket activation — even if a service isn't ready, its socket is listening, so dependent services don't block. The After= directive doesn't mean 'wait until it's started' — it means 'this unit should be in the dependency graph after the other.' Actual ordering is managed by Type=notify or Requires= with After=.'<br><br><strong>Q:</strong> 'How would you debug a service that keeps crashing on startup?'<br><br><strong>A:</strong> 'First, run <code>journalctl -u &lt;service&gt; -n 50 --no-pager</code> to see the logs. Then check <code>systemctl status &lt;service&gt;</code> for the exit code and recent journal entries. Common causes: missing environment variables, permission issues (check journalctl for 'Permission denied'), or dependency failures. I'd also check <code>systemctl list-dependencies</code> to ensure all required units are running. If the service restarts in a loop, check RestartSec= and increase it to avoid log flooding.'<br><br><strong>Q:</strong> 'What's the difference between systemd and Docker?'<br><br><strong>A:</strong> 'systemd is the init system for the host — it manages system services, mounts, sockets, and the boot process. Docker manages application containers. systemd can also manage containers (systemd-nspawn, podman with systemd integration), but their scope is different. In production, many FAANG services run inside Docker containers on hosts that use systemd — the host systemd manages containerd/dockerd, and inside the container, the application might use a minimal init or no init at all.'",
      "codeBlock": ""
    },
    {
      "title": "References & Further Reading",
      "description": "<ul><li><a href=\"https://www.freedesktop.org/software/systemd/man/latest/\"><b>systemd Documentation</b></a> — Official man pages and guides</li><li><a href=\"https://www.digitalocean.com/community/tutorials/systemd-essentials-working-with-services-timers-and-sockets\"><b>systemd Essentials</b></a> — DigitalOcean tutorial series</li><li><a href=\"https://www.amazon.com/Linux-Systemd-View-Documentation-Integration/dp/1798679658\"><b>Linux systemd: A Practical Guide</b></a> — In-depth reference for system administration</li></ul>",
      "codeBlock": ""
    }
  ],
  "details": "<p class=\"mb-3\"><strong>FAANG Architect's Note:</strong> systemd is one of the most controversial pieces of Linux infrastructure — some love it, some hate it. But at FAANG, we don't care about the philosophical debate. We care that systemd provides a consistent, declarative, and dependency-aware way to manage services across tens of thousands of hosts. The ability to write a correct unit file, understand journald's structured logging, and debug boot issues is table stakes for any production engineer.</p><p><strong>Production Tip:</strong> Always set <code>TimeoutStopSec=</code> in your service units. If your service doesn't stop within the timeout, systemd sends SIGKILL. Database services and stateful applications need extra time to flush buffers. A good pattern: set TimeoutStopSec=120 for databases and 30 for stateless web services. Also, never forget <code>systemctl daemon-reload</code> after editing unit files — one of the most common 'why isn't my change working?' mistakes.</p>",
  "tags": ["FAANG", "Linux", "systemd", "journald", "init", "service management"]
}
