{
  "id": "py-ds-heap",
  "title": "Priority Queues & Heaps",
  "category": "Python",
  "subcategory": "Data Structures",
  "description": "Heaps are the backbone of scheduling, Dijkstra's algorithm, and stream processing. Python's heapq is always a min-heap — knowing how to adapt it is a core FAANG skill.",
  "language": "python",
  "sections": [
    {
      "title": "Heap Fundamentals & Complexity",
      "description": "A binary heap is a <strong>complete binary tree</strong> stored as a flat array. For any node at index <code>i</code>: left child at <code>2i+1</code>, right child at <code>2i+2</code>, parent at <code>(i-1)//2</code>. No pointer overhead — cache-friendly.<div class=\"my-5 flex justify-center\"><svg width=\"280\" height=\"150\" viewBox=\"0 0 280 150\" fill=\"none\" xmlns=\"http://www.w3.org/2000/svg\" class=\"max-w-full\"><line x1=\"140\" y1=\"30\" x2=\"90\" y2=\"75\" class=\"stroke-slate-300 dark:stroke-slate-700\" stroke-width=\"1.5\"/><line x1=\"140\" y1=\"30\" x2=\"190\" y2=\"75\" class=\"stroke-slate-300 dark:stroke-slate-700\" stroke-width=\"1.5\"/><line x1=\"90\" y1=\"80\" x2=\"60\" y2=\"125\" class=\"stroke-slate-300 dark:stroke-slate-700\" stroke-width=\"1.5\"/><line x1=\"90\" y1=\"80\" x2=\"120\" y2=\"125\" class=\"stroke-slate-300 dark:stroke-slate-700\" stroke-width=\"1.5\"/><line x1=\"190\" y1=\"80\" x2=\"165\" y2=\"125\" class=\"stroke-slate-300 dark:stroke-slate-700\" stroke-width=\"1.5\"/><circle cx=\"140\" cy=\"30\" r=\"18\" class=\"fill-brand-500/10 stroke-brand-500\" stroke-width=\"1.5\"/><text x=\"140\" y=\"34\" text-anchor=\"middle\" class=\"fill-brand-600 dark:fill-brand-400 font-mono text-xs font-bold\">5</text><circle cx=\"90\" cy=\"80\" r=\"16\" class=\"fill-slate-50 dark:fill-slate-800/40 stroke-slate-200 dark:stroke-slate-700\" stroke-width=\"1.5\"/><text x=\"90\" y=\"84\" text-anchor=\"middle\" class=\"fill-slate-700 dark:fill-slate-300 font-mono text-xs\">10</text><circle cx=\"190\" cy=\"80\" r=\"16\" class=\"fill-slate-50 dark:fill-slate-800/40 stroke-slate-200 dark:stroke-slate-700\" stroke-width=\"1.5\"/><text x=\"190\" y=\"84\" text-anchor=\"middle\" class=\"fill-slate-700 dark:fill-slate-300 font-mono text-xs\">15</text><circle cx=\"60\" cy=\"130\" r=\"12\" class=\"fill-slate-50 dark:fill-slate-800/40 stroke-slate-200 dark:stroke-slate-700\" stroke-width=\"1.2\"/><text x=\"60\" y=\"134\" text-anchor=\"middle\" class=\"fill-slate-500 dark:fill-slate-400 font-mono text-[10px]\">30</text><circle cx=\"120\" cy=\"130\" r=\"12\" class=\"fill-slate-50 dark:fill-slate-800/40 stroke-slate-200 dark:stroke-slate-700\" stroke-width=\"1.2\"/><text x=\"120\" y=\"134\" text-anchor=\"middle\" class=\"fill-slate-500 dark:fill-slate-400 font-mono text-[10px]\">40</text><circle cx=\"165\" cy=\"130\" r=\"12\" class=\"fill-slate-50 dark:fill-slate-800/40 stroke-slate-200 dark:stroke-slate-700\" stroke-width=\"1.2\"/><text x=\"165\" y=\"134\" text-anchor=\"middle\" class=\"fill-slate-500 dark:fill-slate-400 font-mono text-[10px]\">20</text><text x=\"140\" y=\"148\" text-anchor=\"middle\" class=\"fill-slate-400 dark:fill-slate-500 font-mono text-[9px]\">array: [5, 10, 15, 30, 40, 20]</text></svg></div>",
      "codeBlock": "import heapq\n\n# heapify — O(N) (not O(N log N) — uses sift-down, not N pushes)\nheap = [40, 10, 30, 5, 15]\nheapq.heapify(heap)\nprint(heap[0])          # 5 — always the minimum\n\n# push — O(log N)\nheapq.heappush(heap, 2)\n\n# pop minimum — O(log N)\nsmallest = heapq.heappop(heap)   # 2\n\n# pushpop — O(log N), faster than separate push+pop\nresult = heapq.heappushpop(heap, 1)  # pushes 1, pops minimum\n\n# nlargest / nsmallest — O(N log K)\ntop3 = heapq.nlargest(3, [3,1,4,1,5,9,2,6])  # [9, 6, 5]\n# ponytail: use sorted()[-k:] for K close to N; heapq for small K"
    },
    {
      "title": "Max-Heap & Custom Priority",
      "description": "Python only supports min-heaps. For max-heap: negate values. For custom priority: push tuples — Python compares lexicographically. This is the standard FAANG interview technique.",
      "codeBlock": "import heapq\n\n# Max-heap via negation\nmax_heap = []\nfor val in [3, 1, 4, 1, 5, 9]:\n    heapq.heappush(max_heap, -val)\n\nprint(-heapq.heappop(max_heap))  # 9 (max)\n\n# Custom priority with tuples: (priority, item)\n# Lower number = higher priority (min-heap semantics)\ntasks = []\nheapq.heappush(tasks, (3, 'low priority task'))\nheapq.heappush(tasks, (1, 'urgent task'))\nheapq.heappush(tasks, (2, 'medium task'))\n\npriority, task = heapq.heappop(tasks)  # (1, 'urgent task')\n\n# Tie-breaking: add a counter to avoid comparing non-comparable objects\nimport itertools\ncounter = itertools.count()\nheapq.heappush(tasks, (1, next(counter), {'name': 'task_obj'}))  # never compares dicts"
    },
    {
      "title": "Top-K Elements & K-Way Merge",
      "description": "<strong>Top-K pattern:</strong> Maintain a min-heap of size K while scanning. If current element > heap top, replace. Final heap contains the K largest. O(N log K) — much better than sort O(N log N) when K ≪ N.<br><br><strong>K-Way Merge:</strong> Merge K sorted arrays efficiently. Push the first element of each array onto a min-heap, then repeatedly pop the min and push the next element from that array. O(N log K) where N = total elements.",
      "codeBlock": "import heapq\n\n# === Top-K Largest Elements — O(N log K) ===\ndef top_k_largest(nums, k):\n    heap = nums[:k]\n    heapq.heapify(heap)         # min-heap of size K\n    for num in nums[k:]:\n        if num > heap[0]:       # larger than current minimum\n            heapq.heapreplace(heap, num)  # faster than pop+push\n    return sorted(heap, reverse=True)\n\n# === Kth Largest Element — O(N log K) ===\ndef kth_largest(nums, k):\n    return heapq.nlargest(k, nums)[-1]  # O(N log K)\n    # Or: maintain min-heap of size K (same complexity, same idea)\n\n# === K-Way Merge — O(N log K) ===\ndef merge_k_sorted(arrays):\n    result = []\n    # heap stores (value, array_index, element_index)\n    heap = []\n    for i, arr in enumerate(arrays):\n        if arr:\n            heapq.heappush(heap, (arr[0], i, 0))\n\n    while heap:\n        val, arr_i, elem_i = heapq.heappop(heap)\n        result.append(val)\n        if elem_i + 1 < len(arrays[arr_i]):\n            next_val = arrays[arr_i][elem_i + 1]\n            heapq.heappush(heap, (next_val, arr_i, elem_i + 1))\n    return result\n\n# Example\narrs = [[1, 4, 7], [2, 5, 8], [3, 6, 9]]\nprint(merge_k_sorted(arrs))  # [1,2,3,4,5,6,7,8,9]"
    },
    {
      "title": "Median of a Data Stream — Two Heaps",
      "description": "Classic FAANG hard problem. Maintain two heaps: a <strong>max-heap for the lower half</strong> and a <strong>min-heap for the upper half</strong>. Keep them balanced (sizes differ by at most 1). Median is either the top of the larger heap or the average of both tops. All operations O(log N).",
      "codeBlock": "import heapq\n\nclass MedianFinder:\n    def __init__(self):\n        self.lo = []   # max-heap (lower half) — negate for max-heap\n        self.hi = []   # min-heap (upper half)\n\n    def add_num(self, num: int) -> None:\n        # Always push to lo first\n        heapq.heappush(self.lo, -num)\n\n        # Maintain max(lo) <= min(hi)\n        if self.hi and -self.lo[0] > self.hi[0]:\n            heapq.heappush(self.hi, -heapq.heappop(self.lo))\n\n        # Rebalance sizes: lo can have at most 1 more element\n        if len(self.lo) > len(self.hi) + 1:\n            heapq.heappush(self.hi, -heapq.heappop(self.lo))\n        elif len(self.hi) > len(self.lo):\n            heapq.heappush(self.lo, -heapq.heappop(self.hi))\n\n    def find_median(self) -> float:\n        if len(self.lo) > len(self.hi):\n            return -self.lo[0]          # odd total → lo has one extra\n        return (-self.lo[0] + self.hi[0]) / 2.0  # even total → average tops\n\nmf = MedianFinder()\nfor n in [1, 2, 3, 4, 5]:\n    mf.add_num(n)\n    print(mf.find_median())  # 1.0, 1.5, 2.0, 2.5, 3.0"
    },
    {
      "title": "Dijkstra's Algorithm — Heap-Based",
      "description": "Dijkstra's finds shortest paths from a source to all nodes in a weighted graph with non-negative edges. Using a min-heap gives O((V + E) log V). Always use <code>heapq</code> — the priority queue is the core of the algorithm.",
      "codeBlock": "import heapq\nfrom collections import defaultdict\n\ndef dijkstra(graph, src):\n    \"\"\"\n    graph: {node: [(weight, neighbor), ...]}\n    Returns: dist dict — shortest distance from src to every node\n    \"\"\"\n    dist = defaultdict(lambda: float('inf'))\n    dist[src] = 0\n    heap = [(0, src)]   # (distance, node)\n\n    while heap:\n        d, u = heapq.heappop(heap)\n        if d > dist[u]:\n            continue    # stale entry — skip (lazy deletion)\n        for weight, v in graph[u]:\n            if dist[u] + weight < dist[v]:\n                dist[v] = dist[u] + weight\n                heapq.heappush(heap, (dist[v], v))\n    return dict(dist)\n\n# Example: weighted graph\ngraph = {\n    'A': [(1, 'B'), (4, 'C')],\n    'B': [(1, 'A'), (2, 'C'), (5, 'D')],\n    'C': [(4, 'A'), (2, 'B'), (1, 'D')],\n    'D': [(5, 'B'), (1, 'C')]\n}\nprint(dijkstra(graph, 'A'))  # {'A': 0, 'B': 1, 'C': 3, 'D': 4}"
    }
  ],
  "details": "<p class=\"mb-3\"><strong>heapify is O(N), not O(N log N):</strong> This surprises many. Building a heap by repeatedly calling heappush is O(N log N). But heapify uses a bottom-up sift-down approach — most nodes are near the bottom (short sift distances). The total work sums to O(N) by the geometric series. Always use heapify when converting an existing list.</p><p class=\"mb-3\"><strong>Lazy Deletion:</strong> Instead of removing arbitrary elements from a heap (no native support, would be O(N)), mark them as 'deleted' in a set and skip them when popped. This is how real schedulers handle cancellations. O(log N) per operation, O(1) extra per entry.</p><p class=\"mb-3\"><strong>When to use a heap vs sorted list:</strong> Heap is better when you only need the min/max repeatedly (O(log N) per op). Sorted list (via bisect) is better when you need arbitrary rank queries or need to iterate in order. For a fully sorted stream, consider sortedcontainers.SortedList from PyPI.</p>"
}
