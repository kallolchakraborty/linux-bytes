{
  "id": "py-security",
  "language": "python",
  "title": "Python Security Patterns",
  "category": "Python",
  "subcategory": "Advanced & FAANG",
  "description": "Python security from basics to Staff+ FAANG depth: injection prevention, SAST, dependency scanning, secrets management, pickle risks, sandboxing, and supply chain security.",
  "sections": [
    {
      "title": "1. Injection Prevention — SQL, Command, and Template Injection",
      "description": "Python applications are vulnerable to injection when untrusted input is concatenated into SQL, shell commands, or templates. Use parameterized queries, <code>shlex.quote()</code>, and safe templates.<br><br><strong>SQL injection:</strong> Never use f-strings for SQL. Use <code>cursor.execute('SELECT * FROM users WHERE id = %s', [user_id])</code> with param substitution. For ORMs (SQLAlchemy), always use the ORM's parameterized interface.<br><br><strong>Command injection:</strong> Never use <code>os.system()</code> or <code>subprocess(shell=True)</code> with user input. Use <code>subprocess.run(['ls', '-l', filename])</code> with <code>shell=False</code> (default).<br><br><strong>Template injection (SSTI):</strong> Never render user-controlled strings through Jinja2 with <code>autoescape=False</code>. Use <code>autoescape=True</code> and validate template names against an allowlist.<br><br><strong>FAANG Staff+ angle:</strong> 'Your team has 50 Python services. How do you ensure no SQL injection anywhere?' \u2014 Static analysis in CI (semgrep/bandit) + mandatory parameterized queries in code review + quarterly penetration testing.",
      "codeBlock": "# === SQL injection SAFE ===\ndef get_user_safe(user_id: int) -> dict | None:\n    cur = conn.cursor()\n    cur.execute('SELECT * FROM users WHERE id = %s', (user_id,))\n    return cur.fetchone()\n\n# === SQL injection UNSAFE ===\ndef get_user_unsafe(user_id: str) -> dict | None:\n    cur = conn.cursor()\n    cur.execute(f'SELECT * FROM users WHERE id = {user_id}')  # DANGEROUS\n    return cur.fetchone()\n\n# === Command injection SAFE ===\nimport subprocess\nsubprocess.run(['ls', '-l', user_provided_path], check=True)  # shell=False is default\n\n# === Command injection UNSAFE ===\nimport os\nos.system(f'ls -l {user_provided_path}')  # DANGEROUS: shell interprets\n\n# === Template injection safe ===\nfrom jinja2 import Environment, select_autoescape\nenv = Environment(autoescape=True)\ntemplate = env.from_string('<h1>{{ title }}</h1>')\nprint(template.render(title='<script>alert(1)</script>'))  # escaped\n\n# === shlex for safe shell argument quoting ===\nimport shlex\nsafe_arg = shlex.quote(user_input)\nsubprocess.run(['ssh', 'host', safe_arg], check=True)"
    },
    {
      "title": "2. SAST and Dependency Scanning",
      "description": "<strong>Static Application Security Testing (SAST)</strong> scans source code for vulnerabilities without running it. <strong>bandit</strong> is the standard Python SAST: <code>bandit -r my_project/</code>. It detects hardcoded passwords, SQL injection patterns, shell injection, eval usage, and more.<br><br><strong>Dependency scanning:</strong> <code>pip-audit</code> checks installed packages against known CVEs (Common Vulnerabilities and Exposures). <code>safety</code> checks against a curated vulnerability database. Always run in CI as a blocking check.<br><br><strong>SBOM (Software Bill of Materials):</strong> <code>cyclonedx-bom</code> generates an SPDX/CycloneDX SBOM for your project. Google requires SBOMs for all production services.<br><br><strong>FAANG Staff+ angle:</strong> 'Our Python monorepo has 500+ dependencies. A critical CVE is announced. How do we find and fix it in 24 hours?' \u2014 (1) pip-audit on every service to list affected packages. (2) Automated PR creation via Dependabot/Renovate. (3) Staged rollout with monitoring. (4) Postmortem to improve detection speed.",
      "codeBlock": "# === bandit (SAST) ===\n# $ pip install bandit\n# $ bandit -r src/ -f json -o report.json\n\n# Common findings bandit catches:\n# B101: assert used (assertions disabled with -O)\n# B102: exec used\n# B105: hardcoded password\n# B108: world-writable file\n# B110: try/except/pass\n# B201: flask debug=True\n# B301: pickle\n# B303: md5/sha1 used\n\n# === pip-audit (dependency scanning) ===\n# $ pip install pip-audit\n# $ pip-audit\n# $ pip-audit --fix  # auto-upgrade vulnerable packages\n\n# === Safety ===\n# $ pip install safety\n# $ safety check --full-report\n\n# === SBOM generation ===\n# $ pip install cyclonedx-bom\n# $ cyclonedx-py -e --format json > sbom.json\n\n# === CI integration (GitHub Actions) ===\n# .github/workflows/security.yml\n# name: Security Scan\n# on: [push, pull_request]\n# jobs:\n#   security:\n#     runs-on: ubuntu-latest\n#     steps:\n#       - uses: actions/checkout@v4\n#       - uses: actions/setup-python@v5\n#       - run: pip install bandit pip-audit\n#       - run: bandit -r . -f json | tee bandit.json\n#       - run: pip-audit | tee audit.txt"
    },
    {
      "title": "3. Secrets Management",
      "description": "Never hardcode secrets. Use environment variables, vault services (HashiCorp Vault, AWS Secrets Manager, GCP Secret Manager), or encrypted config files.<br><br><strong>dotenv:</strong> <code>.env</code> files for local dev only \u2014 NEVER commit them (<code>.env</code> in <code>.gitignore</code>).<br><br><strong>Vault:</strong> HashiCorp Vault with short-lived tokens. <code>hvac</code> Python client fetches secrets at startup and on rotation.<br><br><strong>Secret detection:</strong> <code>trufflehog</code> and <code>git-secrets</code> scan git history for committed secrets. Run pre-commit hook to prevent committing secrets.<br><br><strong>FAANG Staff+ angle:</strong> 'Design the secrets strategy for a new Python microservice' \u2014 (1) No hardcoded secrets \u2014 ever. (2) Use Vault with auto-rotation. (3) Encrypt secrets at rest and in transit. (4) Audit all secret access. (5) Rotate keys before they expire. (6) Incident response plan for compromised secrets.",
      "codeBlock": "# === Environment variables (12-factor app) ===\nimport os\nDATABASE_URL = os.environ['DATABASE_URL']  # required, fails fast if missing\nLOG_LEVEL = os.environ.get('LOG_LEVEL', 'INFO')  # optional with default\n\n# NEVER:\n# DATABASE_URL = 'postgres://user:pass@localhost:5432/db'\n\n# === python-dotenv for local dev ===\n# .env file (in .gitignore!)\n# DATABASE_URL=postgres://localhost:5432/dev\n# from dotenv import load_dotenv\n# load_dotenv()  # loads .env for local dev only\n\n# === HashiCorp Vault ===\n# import hvac\n# client = hvac.Client(url='https://vault.example.com', token=os.environ['VAULT_TOKEN'])\n# secret = client.secrets.kv.v2.read_secret_version(path='myapp/db')\n# DATABASE_URL = secret['data']['data']['url']\n\n# === AWS Secrets Manager ===\n# import boto3\n# from botocore.config import Config\n# client = boto3.client('secretsmanager', config=Config(connect_timeout=5))\n# resp = client.get_secret_value(SecretId='myapp/db')\n# DATABASE_URL = json.loads(resp['SecretString'])['url']\n\n# === Pre-commit secret detection ===\n# .pre-commit-config.yaml\n# repos:\n#   - repo: https://github.com/pre-commit/pre-commit-hooks\n#     rev: v4.5.0\n#     hooks:\n#       - id: detect-private-key\n#       - id: check-added-large-files\n#   - repo: https://github.com/Yelp/detect-secrets\n#     rev: v1.4.0\n#     hooks:\n#       - id: detect-secrets"
    },
    {
      "title": "4. Pickle Risks and Safely Deserializing Data",
      "description": "<code>pickle.loads()</code> can execute arbitrary code during deserialization. Never unpickle untrusted data. This is the #1 Python security mistake in FAANG interviews.<br><br><strong>Safe alternatives:</strong> JSON (<code>json.loads</code>), MessagePack (<code>msgpack</code>), Protocol Buffers (<code>protobuf</code>), or Avro. For internal ML models, use <code>safetensors</code> (Hugging Face) which avoids pickle entirely.<br><br><strong>If you must use pickle:</strong> Only between trusted services on a private network. Sign the data with HMAC. Use <code>pickle.Unpickler</code> with a restricted <code>find_class</code>.<br><br><strong>FAANG Staff+ angle:</strong> 'Your ML training pipeline loads model artifacts from a shared filesystem. How do you prevent RCE via malicious pickles?' \u2014 (1) Switch to <code>safetensors</code> or ONNX for model weights. (2) If pickle is unavoidable, use a restricted unpickler that only allows a whitelist of known classes. (3) Sign all artifacts with a service account key. (4) Scan artifacts for anomalies before loading.",
      "codeBlock": "# === UNSAFE: RCE via pickle ===\nimport pickle\n\nclass Exploit:\n    def __reduce__(self):\n        return (os.system, ('rm -rf /',))\n\nmalicious = pickle.dumps(Exploit())\npickle.loads(malicious)  # EXECUTES rm -rf / !\n\n# === SAFER: restricted unpickler ===\nimport io\nimport pickletools\n\nclass SafeUnpickler(pickle.Unpickler):\n    ALLOWED_CLASSES = {\n        'builtins.dict', 'builtins.list', 'builtins.str',\n        'builtins.int', 'builtins.float', 'builtins.bytes',\n        'collections.OrderedDict',\n    }\n\n    def find_class(self, module, name):\n        qualified = f'{module}.{name}'\n        if qualified not in self.ALLOWED_CLASSES:\n            raise pickle.UnpicklingError(f'forbidden class: {qualified}')\n        return super().find_class(module, name)\n\ndef safe_load(data: bytes):\n    return SafeUnpickler(io.BytesIO(data)).load()\n\n# === SAFEST: use safetensors ===\n# from safetensors.torch import load_file\n# weights = load_file('model.safetensors')  # no RCE risk\n\n# === Signature verification ===\nimport hmac\nimport hashlib\n\ndef verify_signature(data: bytes, signature: bytes, key: bytes) -> bool:\n    expected = hmac.new(key, data, hashlib.sha256).digest()\n    return hmac.compare_digest(signature, expected)"
    },
    {
      "title": "5. Sandboxing and Process Isolation",
      "description": "Python has no native sandbox. <code>eval()</code>/<code>exec()</code> with user input is dangerous even with restricted globals \u2014 there are known escapes using <code>().__class__.__bases__[0].__subclasses__()</code>.<br><br><strong>Subprocess isolation:</strong> Run untrusted Python code in a separate process via <code>subprocess</code> with resource limits (CPU, memory, timeout). Use Docker or gVisor for stronger isolation.<br><br><strong>PyPy sandbox:</strong> PyPy has a sandbox mode that intercepts all system calls. Slower but safer than CPython for untrusted code execution.<br><br><strong>FAANG Staff+ angle:</strong> 'Design a code execution service that runs user-submitted Python safely' \u2014 (1) gVisor or Firecracker micro-VM per execution. (2) Resource limits (CPU quota, memory limit, wall-clock timeout). (3) No network access. (4) Read-only filesystem. (5) Kill on any forbidden syscall. (6) Stateless — no persistence between executions.",
      "codeBlock": "# === subprocess isolation with resource limits ===\nimport subprocess\nimport resource\n\ndef run_untrusted(code: str, timeout=5, max_mem=100*1024*1024):\n    def set_limits():\n        resource.setrlimit(resource.RLIMIT_AS, (max_mem, max_mem))\n        resource.setrlimit(resource.RLIMIT_CPU, (timeout, timeout))\n\n    proc = subprocess.run(\n        ['python3', '-c', code],\n        capture_output=True,\n        timeout=timeout,\n        preexec_fn=set_limits,  # set limits IN the child process\n    )\n    return proc.stdout.decode(), proc.stderr.decode()\n\n# === eval() is NOT safe, even with restricted globals ===\ndef eval_safe(expr: str) -> Any:\n    # STILL UNSAFE \u2014 can escape via __subclasses__\n    return eval(expr, {'__builtins__': {}}, {})  # DANGEROUS\n\n# Known escape:\n# eval_safe(\"().__class__.__bases__[0].__subclasses__()\")\n# Returns all subclasses of object \u2014 including file readers, os, etc.\n\n# === Docker for strong isolation ===\n# $ docker run --rm --read-only --network=none \\\n#   --memory=100m --cpus=1 --pids-limit=50 \\\n#   python:3.12-slim python3 -c \"your code here\""
    },
    {
      "title": "6. Staff+ Security Architecture",
      "description": "At Staff+ level, security is a design constraint, not a checklist item.<br><br><strong>Threat modeling:</strong> Use STRIDE (Spoofing, Tampering, Repudiation, Information Disclosure, Denial of Service, Elevation of Privilege) per service. Document threats and mitigations in design docs.<br><br><strong>Supply chain security:</strong> Pin dependency versions (<code>pip freeze > requirements.txt</code>), use hash checking (<code>--require-hashes</code>), mirror to internal registry, scan all dependencies in CI. <strong>SLSA framework</strong> (Supply-chain Levels for Software Artifacts) at level 2+ is expected at FAANG.<br><br><strong>Incident response:</strong> Every security issue gets a severity (SEV-1 through SEV-4). SEV-1 (active data breach): <5min to acknowledge, <30min to mitigate. Postmortem within 72 hours.<br><br><strong>FAANG Staff+ angle:</strong> 'Design the security strategy for a Python-based ML training platform' \u2014 (1) Network isolation for training clusters (no public IPs). (2) Encrypted model artifacts at rest and in transit. (3) Signed container images with admission control. (4) Audit logging for all data access. (5) Penetration testing before each major launch. (6) Bug bounty program for external researchers.",
      "codeBlock": "# SLSA framework: build integrity\n# Level 1: Build script exists\n# Level 2: Build runs on a hosted CI; provenance generated\n# Level 3: Hardened CI (ephemeral, isolated); provenance signed\n# Level 4: Two-person review of all build changes; hermetic builds\n\n# Dependency pinning with hash verification\n# requirements.txt (example):\n# flask==3.0.0 --hash=sha256:abc...\n# $ pip install --require-hashes -r requirements.txt\n\n# Supply chain verification script\nimport hashlib\nimport json\n\ndef verify_dependency_integrity(package, version, expected_hash):\n    import importlib.metadata\n    dist = importlib.metadata.distribution(package)\n    # Verify installed package hash matches expected\n    actual = hashlib.sha256(dist.read_text('RECORD').encode()).hexdigest()\n    if actual != expected_hash:\n        raise ValueError(f'Integrity check failed for {package} {version}')\n\n# Pre-commit security hooks\n# .pre-commit-config.yaml\n# repos:\n#   - repo: https://github.com/PyCQA/bandit\n#     rev: 1.7.7\n#     hooks:\n#       - id: bandit\n#         args: ['-r', 'src/', '-ll']"
    },
    {
      "title": "References & Further Reading",
      "description": "<ul><li><a href=\"https://bandit.readthedocs.io\"><b>bandit</b></a> — Python SAST tool</li><li><a href=\"https://pypi.org/project/pip-audit/\"><b>pip-audit</b></a> — Dependency vulnerability scanner</li><li><a href=\"https://safetycli.com\"><b>Safety CLI</b></a> — Python dependency security checker</li><li><a href=\"https://www.python.org/dev/peps/pep-0506/\"><b>PEP 506</b></a> — Python Security Model</li><li><b>OWASP Python Security Cheat Sheet</b></li><li><b>Black Hat Python (2nd ed.)</b> — Chapters on Python exploitation and defense</li></ul>",
      "codeBlock": ""
    }
  ],
  "details": "<p class=\"mb-3\"><strong>Security workflow:</strong> (1) Threat model every service (STRIDE). (2) Run SAST in CI (bandit). (3) Scan dependencies (pip-audit). (4) Never hardcode secrets. (5) Never unpickle untrusted data. (6) Pin dependencies with hash verification. (7) Incident response plan for each severity level.</p><p class=\"mb-3\"><strong>Common mistakes:</strong> Hardcoding secrets 'just for testing'; using eval/exec with user input; unpickling model artifacts from network storage; running without SAST in CI; not pinning supply chain dependencies; SQL injection via f-strings.</p><p class=\"mb-3\"><strong>Staff+ expectations:</strong> Design the security architecture for the service, not just the security features. Threat model at design time. Automate security scanning in CI. Write incident response playbooks. Mentor the team on secure coding patterns. Know that security is not a sprint — it's a continuous investment in process and culture.</p>",
  "codeBlock": "",
  "tags": [
    "FAANG",
    "Python",
    "security",
    "Staff+",
    "advanced"
  ]
}
