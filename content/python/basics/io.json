{
  "id": "py-basics-io",
  "title": "Input &amp; Output",
  "category": "Python",
  "subcategory": "Basics",
  "description": "Reading user input, printing output, string formatting, and I/O internals — buffering, file descriptors, encoding pitfalls, and memory-mapped I/O. FAANG-depth coverage of Python's I/O stack.",
  "language": "python",
  "codeBlock": "",
  "details": "<p class=\"mb-3\">Python’s I/O stack has three layers: (1) <b>raw file descriptors</b> (os-level, unbuffered), (2) <b>buffered streams</b> (io.BufferedReader, io.BufferedWriter), and (3) <b>text streams</b> (io.TextIOWrapper with encoding/decoding). The built-in <code>open()</code> function abstracts all three layers based on the <code>mode</code> parameter.</p><p class=\"mb-3\"><b>Buffering matters.</b> By default, text mode uses line buffering (<code>buffering=1</code>, flushes on newline), binary mode uses a buffer of <code>io.DEFAULT_BUFFER_SIZE</code> (8192 bytes). Set <code>buffering=0</code> for unbuffered (writes go straight to the kernel — useful for logging crashes). Set <code>buffering=-1</code> for the default. A large buffer reduces syscall overhead but increases memory and data loss risk on crash.</p><p class=\"mb-3\"><b>Encoding is the silent killer.</b> Python 3 uses the system encoding (usually UTF-8) by default, but <code>LANG=C</code> or Windows can give ASCII/latin-1. Always pass <code>encoding='utf-8'</code> explicitly in production code. The <code>errors</code> parameter controls decoding failures: <code>'strict'</code> (raise), <code>'replace'</code> (), <code>'backslashreplace'</code> (\\xNN), <code>'surrogateescape'</code> (file system paths).</p><p class=\"mb-3\"><b>File descriptors &amp; resource limits.</b> Each <code>open()</code> consumes an OS file descriptor. <code>ulimit -n</code> caps the count (typically 256–unlimited). Leaking descriptors causes “Too many open files”. Always use context managers (<code>with open(...) as f:</code>) which call <code>f.__exit__</code> → <code>f.close()</code> even on exceptions. For long-running servers, consider <code>cloexec</code> flags to prevent fd leaks across <code>fork()</code>.</p><p class=\"mb-3\"><b>Memory-mapped I/O</b> via <code>mmap</code> maps a file directly into virtual memory, avoiding syscall overhead for reads/writes. The OS manages paging transparently. This is useful for large files (GB+) where random access is needed — the kernel handles the on-demand page loads.</p>",
  "sections": [
    {
      "title": "Basic Output (print) — Buffers & Streams",
      "description": "<p>The <code>print()</code> function writes to <code>sys.stdout</code>, a text stream that wraps the underlying file descriptor 1. The <code>file</code> argument redirects to any object with a <code>.write()</code> method. For real-time output (log tailing, subprocess), flush explicitly: <code>print(..., flush=True)</code> or set <code>sys.stdout.reconfigure(line_buffering=True)</code>. Without flushing, output may linger in user-space buffers.</p><div style=\"margin:20px 0;display:flex;justify-content:center\"><svg width=\"400\" height=\"120\" viewBox=\"0 0 400 120\" fill=\"none\" xmlns=\"http://www.w3.org/2000/svg\" style=\"max-width:100%\"><defs><style>.io-box{fill:var(--bg-secondary);stroke:var(--border-default);stroke-width:1.5}.io-edge{stroke:var(--brand-primary);stroke-width:1.5;marker-end:url(#arrow-io)}.io-lbl{font:10px monospace;fill:var(--text-primary);text-anchor:middle;dominant-baseline:central}</style><marker id=\"arrow-io\" viewBox=\"0 0 10 10\" refX=\"6\" refY=\"5\" markerWidth=\"5\" markerHeight=\"5\" orient=\"auto-start-reverse\"><path d=\"M0,2 L8,5 L0,8 z\" fill=\"var(--brand-primary)\"/></marker></defs><rect x=\"10\" y=\"40\" width=\"80\" height=\"40\" rx=\"4\" class=\"io-box\"/><text x=\"50\" y=\"60\" class=\"io-lbl\">stdin (Input)</text><line x1=\"90\" y1=\"60\" x2=\"135\" y2=\"60\" class=\"io-edge\"/><rect x=\"140\" y=\"30\" width=\"110\" height=\"60\" rx=\"4\" class=\"io-box\" style=\"stroke:var(--brand-primary)\"/><text x=\"195\" y=\"60\" class=\"io-lbl\" style=\"font-weight:bold;fill:var(--brand-primary)\">Buffer Stream</text><line x1=\"250\" y1=\"50\" x2=\"295\" y2=\"35\" class=\"io-edge\"/><line x1=\"250\" y1=\"70\" x2=\"295\" y2=\"85\" class=\"io-edge\"/><rect x=\"300\" y=\"10\" width=\"90\" height=\"35\" rx=\"4\" class=\"io-box\" style=\"stroke:var(--svg-success-stroke)\/><text x=\"345\" y=\"27\" class=\"io-lbl\" style=\"fill:var(--svg-success-text);font-weight:bold\">stdout (Output)</text><rect x=\"300\" y=\"75\" width=\"90\" height=\"35\" rx=\"4\" class=\"io-box\" style=\"stroke:var(--svg-error-stroke)\/><text x=\"345\" y=\"92\" class=\"io-lbl\" style=\"fill:var(--svg-error-text);font-weight:bold\">stderr (Error)</text></svg></div>",
      "codeBlock": "# sys.stdout is a TextIOWrapper\nimport sys\nprint(\"Hello\")  # shorthand for sys.stdout.write(\"Hello\\n\")\n\n# Custom separator and end\nprint(\"a\", \"b\", \"c\", sep=\", \", end=\"!\\n\")  # a, b, c!\n\n# Force flush for real-time output\nprint(\"Processing...\", end=\"\", flush=True)\n\n# Redirect to file or stderr\nwith open(\"log.txt\", \"w\") as f:\n    print(\"[ERROR] crash\", file=f)\nprint(\"WARN\", file=sys.stderr)\n\n# Check buffer size\nprint(sys.stdout.buffer)      # BufferedWriter\nprint(sys.stdout.buffer.raw)   # FileIO (raw fd)\n\n# Unbuffered mode:\n# sys.stdout.reconfigure(line_buffering=True)\n# sys.stdout.reconfigure(write_through=True)  # no buffer at all"
    },
    {
      "title": "User Input, Encoding & Edge Cases",
      "description": "<p>The <code>input()</code> function reads a line from <code>sys.stdin</code>, strips the trailing newline, and returns a string. All encoding happens under the hood via <code>sys.stdin.encoding</code>. If the input contains bytes that don’t match the terminal encoding, <code>UnicodeDecodeError</code> is raised. For binary input (piped data, images), read <code>sys.stdin.buffer.read()</code> directly. The <code>input()</code> call is blocking — for timeouts, use <code>sys.stdin</code> with <code>select.select()</code> or <code>threading</code>.</p><div style=\"margin:20px 0;display:flex;justify-content:center\"><svg width=\"460\" height=\"140\" viewBox=\"0 0 460 140\" fill=\"none\" xmlns=\"http://www.w3.org/2000/svg\" style=\"max-width:100%\"><defs><style>.io-box{fill:var(--bg-secondary);stroke:var(--border-default);stroke-width:1.5}.io-edge{stroke:var(--brand-primary);stroke-width:1.5;marker-end:url(#arrow-io2)}.io-lbl{font:10px monospace;fill:var(--text-primary);text-anchor:middle;dominant-baseline:central}</style><marker id=\"arrow-io2\" viewBox=\"0 0 10 10\" refX=\"6\" refY=\"5\" markerWidth=\"5\" markerHeight=\"5\" orient=\"auto-start-reverse\"><path d=\"M0,2 L8,5 L0,8 z\" fill=\"var(--brand-primary)\"/></marker></defs><rect x=\"10\" y=\"30\" width=\"120\" height=\"80\" rx=\"4\" class=\"io-box\"/><text x=\"70\" y=\"50\" class=\"io-lbl\" style=\"font-weight:bold\">Python Program</text><text x=\"70\" y=\"70\" class=\"io-lbl\">1. input() blocks</text><text x=\"70\" y=\"90\" class=\"io-lbl\">3. returns str</text><line x1=\"130\" y1=\"60\" x2=\"215\" y2=\"60\" class=\"io-edge\"/><line x1=\"220\" y1=\"85\" x2=\"135\" y2=\"85\" class=\"io-edge\" style=\"stroke:var(--brand-secondary)\"/><rect x=\"220\" y=\"20\" width=\"220\" height=\"100\" rx=\"4\" class=\"io-box\" style=\"stroke:var(--brand-primary)\"/><text x=\"330\" y=\"35\" class=\"io-lbl\" style=\"font-weight:bold;fill:var(--brand-primary)\">Terminal / Pipe (stdin)</text><text x=\"330\" y=\"60\" class=\"io-lbl\">Waits for user input</text><text x=\"330\" y=\"75\" class=\"io-lbl\">2. reads bytes (eg. UTF-8)</text><text x=\"330\" y=\"95\" class=\"io-lbl\">decodes using sys.stdin</text></svg></div>",
      "codeBlock": "# Standard: returns str, strips trailing \\n\nname = input(\"Enter name: \")\nage = int(input(\"Enter age: \"))  # ValueError if non-numeric\n\n# Secure: wrap in try/except\n# try:\n#     val = int(input())\n# except ValueError:\n#     print(\"Invalid number\")\n\n# Binary input (piped data, e.g. cat image.jpg | python script.py)\nimport sys\nraw_bytes = sys.stdin.buffer.read()\n\n# Non-blocking input check (Unix)\nimport sys, select\nif select.select([sys.stdin], [], [], 0.1)[0]:\n    line = sys.stdin.readline()\nelse:\n    print(\"No input within timeout\")\n\n# Encoding pitfall:\n# export LANG=C && echo \"café\" | python3 -c \"print(input())\"\n# This FAILS — latin-1 encoding can't decode 'é'\n# Fix: PYTHONIOENCODING=utf-8 python3 -c \"print(input())\""
    },
    {
      "title": "F-strings — Performance & Pitfalls",
      "description": "<p>F-strings (PEP 498) are the fastest formatting option in Python 3.6+. They compile to bytecode that constructs the string with <code>BUILD_STRING</code> opcode, avoiding method call overhead. Inside f-strings, arbitrary expressions are evaluated: <code>f\"{fn(x)}\"</code>, <code>f\"{obj.attr}\"</code>, even <code>f\"{x=}\"</code> (debug syntax, Python 3.8+). Security: never embed user-supplied template strings in f-strings — use <code>.format()</code> with safe templates or <code>string.Template</code> instead. F-string expressions are evaluated at runtime with full access to locals/globals — a SQL injection-like risk if the format string itself comes from user input.</p><div style=\"margin:20px 0;display:flex;justify-content:center\"><svg width=\"460\" height=\"140\" viewBox=\"0 0 460 140\" fill=\"none\" xmlns=\"http://www.w3.org/2000/svg\" style=\"max-width:100%\"><defs><style>.io-box{fill:var(--bg-secondary);stroke:var(--border-default);stroke-width:1.5}.io-lbl{font:10px monospace;fill:var(--text-primary);text-anchor:middle;dominant-baseline:central}</style></defs><rect x=\"10\" y=\"15\" width=\"210\" height=\"110\" rx=\"4\" class=\"io-box\" style=\"stroke:var(--brand-secondary)\"/><text x=\"115\" y=\"30\" class=\"io-lbl\" style=\"font-weight:bold;fill:var(--brand-secondary)\">f\"Hello {name}\"</text><text x=\"115\" y=\"55\" class=\"io-lbl\">Evaluated at runtime</text><text x=\"115\" y=\"75\" class=\"io-lbl\">Compiles to: BUILD_STRING</text><text x=\"115\" y=\"95\" class=\"io-lbl\" style=\"fill:var(--brand-secondary)\">Performance: Fast (bytecode)</text><rect x=\"240\" y=\"15\" width=\"210\" height=\"110\" rx=\"4\" class=\"io-box\" style=\"stroke:var(--brand-primary)\"/><text x=\"345\" y=\"30\" class=\"io-lbl\" style=\"font-weight:bold;fill:var(--brand-primary)\">\"Hello {}\".format(name)</text><text x=\"345\" y=\"55\" class=\"io-lbl\">Parses format string</text><text x=\"345\" y=\"75\" class=\"io-lbl\">Invokes class method call</text><text x=\"345\" y=\"95\" class=\"io-lbl\" style=\"fill:var(--brand-primary)\">Performance: Slower (overhead)</text></svg></div>",
      "codeBlock": "name = \"Alice\"\nage = 30\n\n# Debug syntax (3.8+): prints name=value\nprint(f\"{name=}, {age=}\")  # name='Alice', age=30\n\n# Expression evaluation\nprint(f\"{2 * 3 + 1}\")         # 7\nprint(f\"{name.upper()}\")      # ALICE\n\n# Nested f-strings (for dynamic precision)\nprecision = 3\nprint(f\"{3.14159:.{precision}f}\")  # 3.142\n\n# SECURITY: DO NOT do this with user input\n# user_template = \"{__import__('os').system('id')}\"\n# result = f\"{user_template}\"  # DANGEROUS — code execution!\n\n# Safe approach: string.Template\nfrom string import Template\nt = Template(\"Hello $name\")\nprint(t.safe_substitute(name=\"Bob\"))  # ignores unknown vars, no runtime eval"
    },
    {
      "title": "File I/O — Open Modes & Binary Safety",
      "description": "<p>The <code>open()</code> function uses different <b>buffering layers</b>: <code>'r'</code> (text, buffered), <code>'rb'</code> (binary, buffered), <code>'w'</code> (truncate + write), <code>'a'</code> (append, O_APPEND). The <code>'+'</code> modifier enables read+write. On Unix, <code>'a'</code> mode uses <code>O_APPEND</code> — writes go to the EOF atomically even with multiple writers (kernel guarantees it). On Windows, <code>'b'</code> mode avoids CRLF translation. Always close fds — leaking fds shows up as <code>ResourceWarning</code> in debug mode (<code>python -Wd</code>).</p><div style=\"margin:20px 0;display:flex;justify-content:center\"><svg width=\"460\" height=\"140\" viewBox=\"0 0 460 140\" fill=\"none\" xmlns=\"http://www.w3.org/2000/svg\" style=\"max-width:100%\"><defs><style>.io-box{fill:var(--bg-secondary);stroke:var(--border-default);stroke-width:1.5}.io-lbl{font:10px monospace;fill:var(--text-primary);text-anchor:middle;dominant-baseline:central}</style></defs><rect x=\"10\" y=\"10\" width=\"440\" height=\"30\" rx=\"4\" class=\"io-box\" style=\"stroke:var(--brand-primary)\"/><text x=\"230\" y=\"25\" class=\"io-lbl\" style=\"font-weight:bold;fill:var(--brand-primary)\">1. Text Stream (TextIOWrapper) --&gt; Character encoding/decoding</text><rect x=\"10\" y=\"50\" width=\"440\" height=\"30\" rx=\"4\" class=\"io-box\" style=\"stroke:var(--brand-secondary)\"/><text x=\"230\" y=\"65\" class=\"io-lbl\" style=\"font-weight:bold;fill:var(--brand-secondary)\">2. Buffered Stream (BufferedReader/BufferedWriter) --&gt; Mem Buffer (8192 B)</text><rect x=\"10\" y=\"90\" width=\"440\" height=\"30\" rx=\"4\" class=\"io-box\"/><text x=\"230\" y=\"105\" class=\"io-lbl\" style=\"font-weight:bold\">3. Raw Stream (FileIO) --&gt; OS system calls (write/read fd)</text></svg></div>",
      "codeBlock": "# Text mode (default): encoding matters\nwith open(\"data.txt\", \"r\", encoding=\"utf-8\") as f:\n    content = f.read()\n\n# Binary mode: no encoding, no \\n translation\nwith open(\"image.jpg\", \"rb\") as f:\n    raw = f.read()\n\n# Explicit buffer control\nwith open(\"large.bin\", \"wb\", buffering=65536) as f:\n    f.write(data)  # 64KB buffer reduces syscall count\n\n# Unbuffered binary (use for crash-safe logging)\nwith open(\"emergency.log\", \"ab\", buffering=0) as f:\n    f.write(b\"CRASH\\n\")  # immediately visible in tail -f\n\n# File descriptor leak detection\n# python -Wd script.py  # shows ResourceWarning on unclosed files\n\n# os-level fd check:\n# import os\n# os.listdir(f'/proc/{os.getpid()}/fd')  # Linux\n# lsof -p <pid>  # cross-platform"
    },
    {
      "title": "Memory-Mapped I/O (mmap) — Zero-Copy Reads",
      "description": "<p><b>Memory-mapped files</b> (<a href=\"https://docs.python.org/3/library/mmap.html\" target=\"_blank\" class=\"text-brand-500\"><code>mmap</code></a>) map a file into virtual memory, letting you access it as a mutable bytearray. The OS pages data in and out on demand — <b>no syscalls</b> for reads/writes after the initial mmap. Use cases: (1) large files you need to modify in place, (2) shared memory between processes (<code>mmap.MAP_SHARED</code>), (3) fast random access in large datasets (e.g., indices, database files). Tradeoff: mmap consumes virtual address space (limited on 32-bit), and the file must fit in virtual memory (though not all at once in RAM — paging handles that). For sequential reads of huge files, buffered reads may be faster due to prefetching.</p><div style=\"margin:20px 0;display:flex;justify-content:center\"><svg width=\"460\" height=\"140\" viewBox=\"0 0 460 140\" fill=\"none\" xmlns=\"http://www.w3.org/2000/svg\" style=\"max-width:100%\"><defs><style>.io-box{fill:var(--bg-secondary);stroke:var(--border-default);stroke-width:1.5}.io-edge{stroke:var(--brand-primary);stroke-width:1.5;stroke-dasharray:4 4}.io-lbl{font:10px monospace;fill:var(--text-primary);text-anchor:middle;dominant-baseline:central}</style></defs><rect x=\"10\" y=\"20\" width=\"180\" height=\"100\" rx=\"4\" class=\"io-box\" style=\"stroke:var(--brand-secondary)\"/><text x=\"100\" y=\"35\" class=\"io-lbl\" style=\"font-weight:bold;fill:var(--brand-secondary)\">Disk File</text><text x=\"100\" y=\"65\" class=\"io-lbl\">Block 0 (4KB)</text><text x=\"100\" y=\"85\" class=\"io-lbl\">Block 1 (4KB)</text><rect x=\"270\" y=\"20\" width=\"180\" height=\"100\" rx=\"4\" class=\"io-box\" style=\"stroke:var(--brand-primary)\"/><text x=\"360\" y=\"35\" class=\"io-lbl\" style=\"font-weight:bold;fill:var(--brand-primary)\">Virtual Memory Address Space</text><text x=\"360\" y=\"65\" class=\"io-lbl\">Page 0: Read/Write</text><text x=\"360\" y=\"85\" class=\"io-lbl\">Page 1: Read/Write</text><line x1=\"190\" y1=\"65\" x2=\"270\" y2=\"65\" class=\"io-edge\"/><line x1=\"190\" y1=\"85\" x2=\"270\" y2=\"85\" class=\"io-edge\"/><text x=\"230\" y=\"50\" class=\"io-lbl\" style=\"font-size:8px;fill:var(--brand-primary)\">On-demand</text><text x=\"230\" y=\"105\" class=\"io-lbl\" style=\"font-size:8px;fill:var(--brand-primary)\">OS Paging</text></svg></div>",
      "codeBlock": "import mmap\n\nwith open(\"large_data.bin\", \"r+b\") as f:\n    # Map the entire file into memory\n    with mmap.mmap(f.fileno(), 0) as mm:\n        # Access as bytearray — no syscalls!\n        print(mm[:10])          # read first 10 bytes\n        mm[0:4] = b\"DATA\"      # modify in place (writes back to file)\n        pos = mm.find(b\"needle\")  # O(n) search in mapped memory\n        if pos != -1:\n            print(mm[pos:pos+10])\n\n# Shared memory between processes\nimport mmap, os\n\nmm = mmap.mmap(-1, 1024,  # anonymous, no file\n               flags=mmap.MAP_SHARED | mmap.MAP_ANONYMOUS)\npid = os.fork()\nif pid == 0:  # child\n    mm[:5] = b\"hello\"\n    mm.close()\nelse:  # parent\n    os.waitpid(pid, 0)\n    print(mm[:5])  # \"hello\" — shared memory!\n    mm.close()"
    },
    {
      "title": "Serialization — JSON & Protocol Buffers",
      "description": "<p><b>JSON</b> is the standard interchange format. Python’s <code>json</code> module maps: <code>dict ↔ object</code>, <code>list ↔ array</code>, <code>str ↔ string</code>, <code>int/float ↔ number</code>, <code>bool ↔ boolean</code>, <code>None ↔ null</code>. Custom serialization via <code>default</code> callable or subclassing <code>json.JSONEncoder</code>. For numeric performance, <code>orjson</code> (Rust-backed) is 3–×5 faster. For large structured data, <b>Protocol Buffers</b> (protobuf) or <b>Apache Arrow</b> give binary encoding with schemas, zero-copy reads, and cross-language support.</p><div style=\"margin:20px 0;display:flex;justify-content:center\"><svg width=\"460\" height=\"140\" viewBox=\"0 0 460 140\" fill=\"none\" xmlns=\"http://www.w3.org/2000/svg\" style=\"max-width:100%\"><defs><style>.io-box{fill:var(--bg-secondary);stroke:var(--border-default);stroke-width:1.5}.io-edge{stroke:var(--brand-primary);stroke-width:1.5;marker-end:url(#arrow-io6)}.io-lbl{font:10px monospace;fill:var(--text-primary);text-anchor:middle;dominant-baseline:central}</style><marker id=\"arrow-io6\" viewBox=\"0 0 10 10\" refX=\"6\" refY=\"5\" markerWidth=\"5\" markerHeight=\"5\" orient=\"auto-start-reverse\"><path d=\"M0,2 L8,5 L0,8 z\" fill=\"var(--brand-primary)\"/></marker></defs><rect x=\"10\" y=\"30\" width=\"120\" height=\"80\" rx=\"4\" class=\"io-box\"/><text x=\"70\" y=\"55\" class=\"io-lbl\" style=\"font-weight:bold\">Python Object</text><text x=\"70\" y=\"75\" class=\"io-lbl\">{\"id\": 42}</text><line x1=\"130\" y1=\"50\" x2=\"215\" y2=\"35\" class=\"io-edge\"/><line x1=\"130\" y1=\"90\" x2=\"215\" y2=\"105\" class=\"io-edge\" style=\"stroke:var(--brand-secondary)\"/><rect x=\"220\" y=\"10\" width=\"230\" height=\"50\" rx=\"4\" class=\"io-box\" style=\"stroke:var(--brand-primary)\"/><text x=\"335\" y=\"25\" class=\"io-lbl\" style=\"font-weight:bold;fill:var(--brand-primary)\">JSON (UTF-8 Text)</text><text x=\"335\" y=\"40\" class=\"io-lbl\">\"{\\\"id\\\": 42}\" (Human Readable)</text><rect x=\"220\" y=\"75\" width=\"230\" height=\"50\" rx=\"4\" class=\"io-box\" style=\"stroke:var(--brand-secondary)\"/><text x=\"335\" y=\"90\" class=\"io-lbl\" style=\"font-weight:bold;fill:var(--brand-secondary)\">Protobuf (Binary)</text><text x=\"335\" y=\"105\" class=\"io-lbl\">b'\\x08\\x2a' (Compact/Fast)</text></svg></div>",
      "codeBlock": "import json\n\n# Basic read/write\nwith open(\"data.json\") as f:\n    data = json.load(f)\n\nwith open(\"data.json\", \"w\") as f:\n    json.dump(data, f, indent=2, ensure_ascii=False)\n\n# Custom serializer for non-standard types\nfrom datetime import datetime\n\ndef json_serial(obj):\n    if isinstance(obj, datetime):\n        return obj.isoformat()\n    if isinstance(obj, bytes):\n        return obj.hex()\n    raise TypeError(f\"Type {type(obj)} not serializable\")\n\njson.dumps({\"ts\": datetime.now()}, default=json_serial)\n\n# Performance: use orjson for speed\n# import orjson\n# orjson.dumps(data)  # bytes output, ~4x faster than json.dumps\n\n# Protocol Buffers example (requires protobuf compiler):\n# from my_pb2 import MyMessage\n# msg = MyMessage()\n# msg.ParseFromString(data)\n# with open(\"out.bin\", \"wb\") as f:\n#     f.write(msg.SerializeToString())"
    },
    {
      "title": "References & Further Reading",
      "description": "<ul><li><a href=\"https://docs.python.org/3/tutorial/inputoutput.html\"><b>Input and Output</b></a> — Official Python tutorial on I/O</li><li><a href=\"https://docs.python.org/3/library/io.html\"><b>io — Core tools for working with streams</b></a> — Python I/O module reference</li><li><a href=\"https://realpython.com/python-input-output/\"><b>Python Input and Output</b></a> — Real Python guide covering all I/O patterns</li><li><a href=\"https://docs.python.org/3/library/struct.html\"><b>struct — Interpret bytes as packed binary data</b></a> — Relevant for binary I/O interview questions</li><li><b>Fluent Python (2nd ed.)</b> — Chapter 8: I/O, streams, and serialization</li></ul>",
      "codeBlock": ""
    }
  ],
  "tags": [
    "FAANG",
    "Python",
    "I/O"
  ]
}