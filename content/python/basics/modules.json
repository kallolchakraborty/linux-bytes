{
  "id": "py-modules",
  "language": "python",
  "title": "Modules, Packages & Import System",
  "category": "Python",
  "subcategory": "Core Patterns",
  "description": "Python's import system from basics to Staff+ depth: module caching, relative vs absolute imports, namespace packages, lazy loading, and large-scale package architecture.",
  "sections": [
    {
      "title": "1. How Python Finds and Loads Modules",
      "description": "When <code>import foo</code> runs: (1) Check <code>sys.modules</code> cache. (2) Find the module via <code>sys.meta_path</code> finders (Finder -> Loader). (3) Execute the module code (fills the module's namespace). (4) Add the module to <code>sys.modules</code>. (5) Bind <code>foo</code> in the caller's namespace.<br><br><strong>sys.path</strong> controls where finders look. It's initialized from PYTHONPATH, virtualenv site-packages, and the current directory. Modifying it at runtime is a code smell.<br><br><strong>FAANG angle:</strong> 'Why does <code>import foo</code> run the module code only once?' \u2014 caching in <code>sys.modules</code>. Subsequent imports get the cached module object without re-executing.",
      "codeBlock": "import sys\n\n# sys.modules: cache of loaded modules\nprint('math' in sys.modules)  # True (stdlib pre-loaded)\nimport math\nprint(id(math) == id(sys.modules['math']))  # True\n\n# sys.path: module search order\nfor p in sys.path[:3]:\n    print(p)  # current dir, PYTHONPATH, site-packages\n\n# Reload a module (rarely needed, useful in REPL)\nimport importlib\nimportlib.reload(math)  # re-executes module code\n\n# Finding a module's location\nprint(math.__file__)  # '/usr/lib/python3.12/math.cpython-312-darwin.so'\n\n# Check if module is built-in\nimport marshal\nprint(marshal.__name__)  # marshal\nprint(hasattr(marshal, '__file__'))  # False (built-in)"
    },
    {
      "title": "2. Absolute vs Relative Imports \u2014 The Right Choice",
      "description": "<strong>Absolute imports</strong> (<code>from package.submodule import X</code>): explicit, unambiguous, refactor-proof. Use them by default.<br><br><strong>Relative imports</strong> (<code>from .submodule import X</code>): implicit, break on reorganization. Only use when the package name is unwieldy or for deep internal references. NEVER use <code>from ..</code> more than two levels deep \u2014 it's a sign to reorganize.<br><br><strong>__init__.py</strong> controls what <code>from package import *</code> exports (<code>__all__</code>) and can aggregate submodule names for convenience.<br><br><strong>FAANG angle:</strong> 'Why does <code>from . import sibling</code> fail when running a script directly?' \u2014 <code>__package__</code> is not set for <code>__main__</code>. Use <code>python -m package.module</code> instead of <code>python module.py</code>.",
      "codeBlock": "# === package structure ===\n# myapp/\n#   __init__.py\n#   utils/\n#     __init__.py\n#     strings.py\n#   models/\n#     __init__.py\n#     user.py\n#   tests/\n#     __init__.py\n#     test_user.py\n\n# Absolute import (preferred)\n# myapp/models/user.py\nfrom myapp.utils.strings import slugify\n\n# Relative import (acceptable for sibling modules)\n# myapp/tests/test_user.py\nfrom ..models.user import User\n\n# __init__.py aggregation\n# myapp/__init__.py\nfrom .models.user import User\nfrom .utils.strings import slugify\n# Now: from myapp import User, slugify\n\n# __all__ controls * exports\n# myapp/models/__init__.py\n__all__ = ['User', 'Profile']\n\n# Avoid: circular imports\n# Use TYPE_CHECKING guard for type annotations\nfrom __future__ import annotations\nfrom typing import TYPE_CHECKING\nif TYPE_CHECKING:\n    from myapp.models.user import User"
    },
    {
      "title": "3. __pycache__ and Bytecode Caching",
      "description": "Python compiles source to <code>.pyc</code> bytecode files stored in <code>__pycache__</code>. The <code>.pyc</code> file's hash matches the source's hash to detect staleness.<br><br><strong>-B flag:</strong> skip bytecode generation. <strong>PYTHONDONTWRITEBYTECODE=1</strong>: same effect. Useful for read-only filesystems or packaging.<br><br><strong>Bytecode is NOT machine code</strong> \u2014 it's still interpreted by the CPython VM. <code>.pyc</code> loading is ~50% faster than parsing source.<br><br><strong>FAANG angle:</strong> 'How does <code>.pyc</code> invalidation work?' \u2014 CPython stores the source file's modification timestamp and size alongside the bytecode. If the source is newer, the <code>.pyc</code> is regenerated. Use <code>-B</code> for ephemeral containers.",
      "codeBlock": "# Python bytecode compilation\n# Source: hello.py\ndef greet(name):\n    return f'hello {name}'\n\n# After first import:\n# __pycache__/hello.cpython-312.pyc\n\n# Disassemble bytecode\nimport dis\ndis.dis(greet)\n# Output:\n#   2           0 RESUME                   0\n#   3           2 LOAD_CONST               1 ('hello ')\n#               4 LOAD_FAST                0 (name)\n#               6 FORMAT_VALUE             0\n#               8 BUILD_STRING             2\n#              10 RETURN_VALUE\n\n# Skip .pyc generation\n# $ python -B script.py\n# $ PYTHONDONTWRITEBYTECODE=1 python script.py\n\n# Check .pyc location\nfrom myapp.models import user\nprint(user.__pycache__)  # /path/to/myapp/models/__pycache__"
    },
    {
      "title": "4. Namespace Packages",
      "description": "Python 3.3+ allows <strong>namespace packages</strong> \u2014 packages split across multiple directories without <code>__init__.py</code>. Each directory with the same name contributes its modules to a single namespace package.<br><br>Use cases: (1) Splitting a large package across repos (e.g., <code>google.cloud.storage</code> and <code>google.cloud.bigquery</code>). (2) Plugin systems where each plugin is a directory with the same namespace. (3) Monorepo with independently-versioned subpackages.<br><br><strong>Resolving conflict:</strong> If both a regular package (with <code>__init__.py</code>) and a namespace package have the same name, the regular package wins.<br><br><strong>FAANG angle:</strong> 'Your monorepo has <code>infra/metrics</code> and <code>infra/logging</code> in different directories. How do you import both as <code>infra.metrics</code> and <code>infra.logging</code>?' \u2014 namespace package: create <code>infra/</code> directories in each location without <code>__init__.py</code>.",
      "codeBlock": "# Standard regular package (with __init__.py)\n# infra/metrics/__init__.py\n# infra/metrics/gauge.py\n# import infra.metrics.gauge  # works\n\n# Namespace package (without __init__.py)\n# /team-a/infra/logging/\n#   \u2514\u2500\u2500 logger.py\n# /team-b/infra/metrics/\n#   \u2514\u2500\u2500 gauge.py\n\n# Both PYTHONPATH entries resolve under 'infra' namespace\n# import infra.logging.logger\n# import infra.metrics.gauge\n\n# Regular package coexists with namespace\n# If both exist, regular package wins (has __init__.py)\n\n# Creating namespace packages via pkg_resources\nfrom pkgutil import extend_path\n__path__ = extend_path(__path__, __name__)  # legacy approach,\n# modern Python 3.3+ just omits __init__.py"
    },
    {
      "title": "5. Lazy Loading and Large Package Architecture",
      "description": "<strong>Lazy loading</strong> defers importing heavy modules until needed. Reduces startup time. Implement via <code>__getattr__</code> on the module (PEP 562) or <code>importlib.util.LazyLoader</code>.<br><br><strong>Submodule vs top-level:</strong> Import from the module that owns the symbol, not its parent. <code>from pandas import DataFrame</code> (not <code>from pandas.core.frame import DataFrame</code>).<br><br><strong>Monorepo package strategy:</strong> Use <code>setup.py</code> with namespace packages for independently-deployable components. Each component has its own <code>setup.py</code>.<br><br><strong>FAANG Staff+ angle:</strong> 'Design the package structure for a machine learning training framework' \u2014 namespace packages for core/trainer/data/eval modules, lazy loading for heavy dependencies (tensorflow/pytorch), and <code>__all__</code> exports at the top-level.",
      "codeBlock": "# === lazy loading via module __getattr__ (PEP 562) ===\n# heavy/__init__.py\nfrom typing import Any\n\ndef __getattr__(name: str) -> Any:\n    if name == 'ExpensiveModel':\n        from .model import ExpensiveModel\n        return ExpensiveModel\n    raise AttributeError(f'module has no attribute {name}')\n\n# Client code:\n# from heavy import ExpensiveModel  # triggers lazy import\n\n# === importlib lazy loader ===\nfrom importlib.util import LazyLoader\nimport importlib\n\nspec = importlib.util.find_spec('pandas')\npandas = importlib.util.module_from_spec(spec)\nspec.loader.exec_module(pandas)  # normal eager load\n\n# Lazy alternative (rare):\nspec.loader = LazyLoader(spec.loader)\nspec.loader.exec_module(pandas)  # defers sub-imports\n\n# === Large package organizing ===\n# myml/\n#   __init__.py        # imports Model, train, evaluate\n#   model/\n#     __init__.py      # exports BaseModel\n#     layers/\n#     losses/\n#   data/\n#     __init__.py\n#     dataset.py\n#     transforms.py\n#   train/\n#     trainer.py\n#     config.py\n#   evaluate/\n#     metrics.py"
    },
    {
      "title": "References & Further Reading",
      "description": "<ul><li><a href=\"https://docs.python.org/3/reference/import.html\"><b>Python Import System</b></a> \u2014 Official reference</li><li><a href=\"https://docs.python.org/3/library/importlib.html\"><b>importlib module</b></a> \u2014 Import machinery implementation</li><li><a href=\"https://peps.python.org/pep-0420/\"><b>PEP 420</b></a> \u2014 Implicit Namespace Packages</li><li><a href=\"https://peps.python.org/pep-0562/\"><b>PEP 562</b></a> \u2014 Module __getattr__ and __dir__</li></ul>",
      "codeBlock": ""
    }
  ],
  "details": "<p class=\"mb-3\"><strong>Import workflow:</strong> (1) Use absolute imports by default. (2) Prefer <code>python -m package.module</code> over direct <code>python module.py</code>. (3) Use <code>__init__.py</code> to aggregate clean public APIs. (4) Use <code>TYPE_CHECKING</code> to break circular import cycles. (5) Use namespace packages for cross-repo package splitting.</p><p class=\"mb-3\"><strong>Common mistakes:</strong> Running <code>python module.py</code> inside a package (breaks relative imports); circular imports from putting too much in <code>__init__.py</code>; modifying <code>sys.path</code> at runtime; not specifying <code>__all__</code>; using <code>from package import *</code> (pollutes namespace, hides dependencies).</p><p class=\"mb-3\"><strong>Staff+ expectations:</strong> Design the package architecture for the team's monorepo. Use namespace packages for independently-versioned components. Implement lazy loading for startup-time optimization. Ensure clear import boundaries between modules to prevent circular dependencies.</p>",
  "codeBlock": "",
  "tags": [
    "FAANG",
    "Python",
    "modules",
    "core-patterns"
  ]
}
