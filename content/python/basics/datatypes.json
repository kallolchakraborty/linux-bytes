{
  "id": "py-basics-datatypes",
  "title": "Data Types &amp; Operations",
  "category": "Python",
  "subcategory": "Basics",
  "description": "An in-depth look at Python's bitwise operations, implicit/explicit type casting, and advanced memory representation patterns.",
  "language": "python",
  "sections": [
    {
      "title": "Bitwise Operators in Detail",
      "description": "Bitwise operators perform calculations on the binary digits (bits) of integers. Python simulates an infinite two's complement system for integers, meaning there is no sign-bit limit on left shifts, unlike fixed-width platforms.",
      "codeBlock": "a = 60            # 60 = 0011 1100 in binary\nb = 13            # 13 = 0000 1101 in binary\n\n# Bitwise AND (&) - 1 if both bits are 1\nbitwise_and = a & b\nprint(bitwise_and) # 12 (0000 1100)\n\n# Bitwise OR (|) - 1 if at least one bit is 1\nbitwise_or = a | b\nprint(bitwise_or)  # 61 (0011 1101)\n\n# Bitwise XOR (^) - 1 if bits are different\nbitwise_xor = a ^ b\nprint(bitwise_xor) # 49 (0011 0001)\n\n# Bitwise NOT (~) - inverts all bits (equal to -(x+1))\nbitwise_not = ~a\nprint(bitwise_not) # -61 (twos complement representation)\n\n# Bitwise Left Shift (<<) - shifts bits left, filling with zeros\nleft_shift = a << 2\nprint(left_shift)  # 240 (1111 0000)\n\n# Bitwise Right Shift (>>) - shifts bits right, dropping rightmost\nright_shift = a >> 2\nprint(right_shift) # 15 (0000 1111)"
    },
    {
      "title": "Implicit vs. Explicit Type Casting",
      "description": "Python supports two types of casting: Implicit Conversion (automatic type promotion by the interpreter to prevent data loss) and Explicit Casting (forcing a type conversion using built-in constructors).",
      "codeBlock": "# Implicit Casting (Coercion)\ninteger_num = 100\nfloat_num = 20.5\nsum_val = integer_num + float_num\nprint(sum_val)        # 120.5 (integer is implicitly promoted to float)\nprint(type(sum_val))  # <class 'float'>\n\n# Explicit Casting (Conversion)\nstring_digit = \"123\"\nconverted_int = int(string_digit)\nprint(converted_int)  # 123\nprint(type(converted_int)) # <class 'int'>\n\n# Float to Int Casting (Truncation gotcha)\npi_val = 3.99\ntruncated_int = int(pi_val)\nprint(truncated_int)  # 3 (truncates decimal part, does not round)\n\n# Boolean Evaluation (Truthiness)\nprint(bool(0))        # False\nprint(bool(0.0))      # False\nprint(bool(\"\"))       # False (empty string is falsy)\nprint(bool([]))       # False (empty list is falsy)\nprint(bool([1, 2]))   # True"
    },
    {
      "title": "Advanced Bitwise Tricks & Gotchas",
      "description": "Practical applications of bit manipulation include bitmasking for flags, checking parity, fast multiplication/division, and simulating fixed-width integers using bit masks.",
      "codeBlock": "# 1. Bit Masking (Flags & Permissions)\nFLAG_READ = 0b100    # 4\nFLAG_WRITE = 0b010   # 2\nFLAG_EXEC = 0b001    # 1\n\n# Grant Read and Write permissions\ncurrent_perms = FLAG_READ | FLAG_WRITE\nprint(current_perms) # 6 (0b110)\n\n# Verify if Write permission is present\nhas_write = (current_perms & FLAG_WRITE) != 0\nprint(has_write)     # True\n\n# Revoke Write permission using XOR/AND-NOT\ncurrent_perms = current_perms ^ FLAG_WRITE\nprint(current_perms) # 4 (0b100, only Read left)\n\n# 2. Check Even or Odd (Parity check)\nnum = 45\nis_odd = (num & 1) != 0\nprint(is_odd)        # True (odd numbers end in 1 in binary)\n\n# 3. Simulating 8-Bit Unsigned Integer Overflow\n# Since Python integers do not overflow, use a mask to force 8-bit limits\nMASK_8BIT = 0xFF     # 255 in decimal\nraw_val = 250\noverflow_val = (raw_val + 15) & MASK_8BIT\nprint(overflow_val)  # 9 (265 & 255 = 9)\n\n# 4. High-Performance Shifting\nx = 7\nmultiply_by_8 = x << 3\nprint(multiply_by_8) # 56 (equal to 7 * 2^3)\ndivide_by_4 = x >> 2\nprint(divide_by_4)   # 1 (equal to 7 // 2^2)"
    }
  ],
  "details": "<p class=\"mb-3\">Python's approach to integers (<code>int</code>) differs fundamentally from lower-level languages like C or Java. In Python, integers have arbitrary precision (they can grow as large as your system memory allows) and are stored in a sign-magnitude-like structure internally. However, for bitwise operations, Python simulates an infinite two's complement representation. This is why bitwise inversion of zero (<code>~0</code>) returns <code>-1</code>, representing an infinite sequence of leading ones.</p>\n\n<p class=\"mb-3\"><strong>Implicit Casting (Coercion):</strong> Python avoids automatic implicit conversions that might cause unexpected data loss (e.g., you cannot add a string and an integer). However, it does support implicit numeric coercion. When executing an operation between an integer (<code>int</code>) and a float (<code>float</code>), Python automatically promotes the integer to a double-precision float before computing, ensuring no precision is lost.</p>\n\n<p class=\"mb-3\"><strong>Explicit Casting & Gotchas:</strong> Explicit casting is handled via class constructors. When casting float to integer using <code>int()</code>, the value is always truncated toward zero, not rounded. To perform mathematical rounding, use <code>round()</code> or the <code>math.floor()</code> and <code>math.ceil()</code> functions. Boolean casting follows strict truthiness rules: numerical zero values (<code>0</code>, <code>0.0</code>), empty sequences (<code>\"\"</code>, <code>[]</code>, <code>()</code>, <code>{}</code>), and the singleton <code>None</code> evaluate to <code>False</code>, while all other objects evaluate to <code>True</code>.</p>\n\n<p class=\"mb-3\"><strong>Advanced Bit Manipulation:</strong> Bit masks are highly efficient tools for managing boolean states. By assigning flags to power-of-two values (e.g., <code>1</code>, <code>2</code>, <code>4</code>, <code>8</code>), you can store multiple boolean permissions inside a single integer. Bitwise operators (<code>&</code>, <code>|</code>, <code>^</code>, <code>~</code>) allow you to check, set, toggle, and clear these flags in a single instruction. Since Python integers don't overflow, simulating C-style fixed-width integers (like 8-bit, 16-bit, or 32-bit registers) requires explicitly applying bitwise AND masks (like <code>& 0xFF</code> for 8-bit unsigned values) after arithmetic operations.</p>"
}