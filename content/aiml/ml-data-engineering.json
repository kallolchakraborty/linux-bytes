{
  "id": "aiml-ml-data-engineering",
  "title": "Data Engineering for ML at Scale — Pipelines, Dedup & Lineage",
  "category": "AI/ML",
  "subcategory": "Data-Centric",
  "description": "High-quality model training is determined by the size and quality of the dataset. Staff AI Engineers must design pipelines that ingest, deduplicate, version, and validate multi-terabyte datasets (Common Crawl scale) with high throughput and strict quality controls.",
  "language": "python",
  "sections": [
    {
      "title": "Web-Scale Data Pipelines: Ingesting & Filtering Common Crawl",
      "description": "<p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>psychology</span> <strong>Intuition:</strong> Training LLMs requires filtering petabytes of raw web crawls (like Common Crawl) down to high-quality tokens. The filtering pipeline comprises: 1) **Heuristic rules**: removal of empty pages, bad words list filtering, token-to-word count ratios. 2) **Language identification**: using fastText classifier to discard non-target languages. 3) **Quality classification**: training a fast classifier (like logistic regression or tiny fastText model) on reference documents (Wikipedia/books) to keep high-quality text and discard spam.</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>bar_chart</span> <strong>Data Reduction:</strong> A standard filtering pipeline discards $\\approx 80\\%-90\\%$ of raw input documents from the web crawl before it ever reaches training stage, protecting training compute efficiency.</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>search</span> <strong>Interview Question:</strong> 'How would you filter a 10TB web dataset to train a code-generating model?' Answer: 1) Reject files with non-UTF8 characters or sizes > 1MB (likely minified/generated). 2) Keep files with high alphanumeric-to-symbol ratios. 3) Apply heuristics to discard boilerplate code (e.g. standard headers, licenses). 4) Deduplicate near-duplicate code files using MinHash/LSH.</p>",
      "codeBlock": "# Conceptual quality heuristics filter\ndef check_document_quality(text):\n    words = text.split()\n    if len(words) < 50 or len(words) > 100000:\n        return False, \"Length outlier\"\n    \n    # Ratio of unique words (prevent repetition spam)\n    unique_ratio = len(set(words)) / len(words)\n    if unique_ratio < 0.3:\n        return False, \"Repetitive content\"\n        \n    # Word length validation\n    avg_word_len = sum(len(w) for w in words) / len(words)\n    if not (3 <= avg_word_len <= 10):\n        return False, \"Gibberish/unnatural language\"\n        \n    return True, \"Passed\""
    },
    {
      "title": "Large-Scale Data Deduplication: MinHash & LSH",
      "description": "<p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>psychology</span> <strong>Intuition:</strong> Exact deduplication (hash matches) is simple, but web crawls contain millions of near-duplicates (pages with different headers/ads). **MinHash** represents documents as sets of n-grams, computes min-hash signatures using family of hash functions, and uses **Locality Sensitive Hashing (LSH)** to group similar signatures into bands/buckets. If two documents collide in at least one band, they are candidate duplicates, allowing $O(N)$ Jaccard similarity estimation instead of $O(N^2)$ pairwise checks.</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>bar_chart</span> <strong>Compute Efficiency:</strong> Deduplicating 100M documents pairwise requires $10^{16}$ comparisons. MinHash + LSH reduces candidate pairs to $\\approx 10^7$ comparisons, making it computationally feasible on standard Spark/Ray clusters.</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>track_changes</span> <strong>Implementation Detail:</strong> Set the number of signature hashes $K$ and bands $B$ to target a specific Jaccard similarity threshold (e.g., $s \\approx 0.85$ matches duplicates with minor edits).</p>",
      "codeBlock": "import hashlib\nimport numpy as np\n\nclass MinHash:\n    def __init__(self, num_hashes=128):\n        self.num_hashes = num_hashes\n        # Generate random coefficients for hash functions: h(x) = (ax + b) % c\n        self.a = np.random.randint(1, 100000, size=num_hashes)\n        self.b = np.random.randint(0, 100000, size=num_hashes)\n        self.prime = 4294967291  # Large prime\n        \n    def get_signature(self, shingle_hashes):\n        \"\"\"\n        Computes MinHash signature for a list of shingles (token hashes).\n        \"\"\"\n        sig = np.full(self.num_hashes, np.inf)\n        for h_val in shingle_hashes:\n            # Apply family of hash functions\n            h_funcs = (self.a * h_val + self.b) % self.prime\n            sig = np.minimum(sig, h_funcs)\n        return sig\n        \n# Two documents sharing similar Jaccard index will produce similar signatures\n# LSH divides these signatures into bands (e.g. 16 bands of 8 hashes) to find candidates"
    },
    {
      "title": "Orchestration & Compute: Beam, Spark & Ray Data",
      "description": "<p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>psychology</span> <strong>Intuition:</strong> Decouple computing frameworks by workload profile. **Apache Spark** is optimal for ETL pipeline structures, massive SQL transformations, and structured raw-to-refined datasets. **Apache Beam** (Google Dataflow) is suited for hybrid batch/streaming workloads where time-window aggregates are needed. **Ray Data** is the industry standard for feeding tensors directly into GPUs, streaming batches during model training because it integrates natively with PyTorch/JAX memory layouts without JVM-to-Python serialization bottlenecks.</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>track_changes</span> <strong>Staff Recommendation:</strong> Use Spark or Beam to clean, filter, and deduplicate data from web crawls down to parquet formats. Then, use Ray Data during the training loop to load, shuffle, tokenize, and stream batches to H100 nodes asynchronously.</p>",
      "codeBlock": "# Conceptual training data pipeline using Ray Data\n# import ray\n# ds = ray.data.read_parquet(\"s3://ml-bucket/tokenized_data/\")\n# ds = ds.map_batches(tokenize_batch, batch_size=1024)\n# ds = ds.random_shuffle()\n# for batch in ds.iter_torch_batches(batch_size=32):\n#     model.train_on_batch(batch)"
    },
    {
      "title": "FAANG Staff+ Q&A: ML Data Engineering",
      "description": "<p><strong>Q: What is data contamination in LLM pre-training and how do you prevent it?</strong><br><strong>A:</strong> Data contamination occurs when test benchmarks (e.g., MMLU, GSM8K questions/answers) are present in the training corpus, leading to falsely high benchmark scores. Prevent it by: 1) Building a bloom filter or suffix-tree index of all benchmark questions. 2) Scanning pre-training datasets for 13-gram exact matches with benchmark files. 3) Removing contaminated documents or splitting training splits so that contaminated files are never shown to the model during training.</p><p><strong>Q: How do you partition streaming pipelines to update online features under 10 seconds?</strong><br><strong>A:</strong> Deploy a streaming engine (Apache Flink or Spark Streaming) processing event logs from a message broker (Kafka). Partition the topic by the primary entity key (e.g., user_id) to avoid out-of-order execution. Maintain localized feature state using rocksDB state backend, and write micro-batch delta aggregates (e.g., view_count_10m) directly to Redis, bypassing analytical data lakes for the online path.</p>",
      "codeBlock": ""
    }
  ],
  "details": "<p><strong>Staff Engineer Summary:</strong> Modern AI value is tied to data pipeline execution. Standardize pipeline steps into clear layers: ingest raw crawls, filter heuristics, deduplicate using MinHash/LSH, clean benchmarks, store in columnar Delta/Parquet tables, and stream to GPU nodes via Ray Data to maximize model FLOP utilization (MFU).</p>",
  "codeBlock": "",
  "tags": ["FAANG", "data-centric", "mlops", "dedup", "pipelines"]
}
