{
  "id": "genai-observability",
  "title": "LLM Observability — Drift Detection, Metrics & Observability at Scale",
  "category": "Gen AI",
  "subcategory": "Production & Optimization",
  "description": "LLM observability is complex because outputs are unstructured and non-deterministic. Staff Engineers must build systems that monitor output quality, attribute costs, track model updates, and calculate statistical drift (PSI, KS test) in real-time.",
  "language": "python",
  "sections": [
    {
      "title": "The Three Pillars of LLM Observability",
      "description": "<p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>psychology</span> <strong>Intuition:</strong> Observability requires mapping traditional telemetry (metrics, logs, traces) to LLM structures. Log input-output pairs; trace the RAG, tool, and safety checks; monitor tokens, latency (TTFT, TPOT), and cost.</p>",
      "codeBlock": "# Traditional + LLM-specific telemetry wrapper\ndef log_llm_metrics(prompt, response, latency, tokens_in, tokens_out):\n    return {\n        \"latency_ms\": latency,\n        \"total_tokens\": tokens_in + tokens_out,\n        \"cost_usd\": (tokens_in * 0.0015 + tokens_out * 0.002) / 1000\n    }"
    },
    {
      "title": "Statistical Drift Detection: PSI, KS Test & JSD",
      "description": "<p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>psychology</span> <strong>Intuition:</strong> Over time, model behavior changes due to provider updates or user base shifts. We detect this by measuring distribution changes using **Population Stability Index (PSI)**, **Kolmogorov-Smirnov (KS) test**, and **Jensen-Shannon Divergence (JSD)**.</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>bar_chart</span> <strong>PSI threshold:</strong> PSI < 0.1 indicates no change, 0.1 to 0.25 indicates moderate shift, and > 0.25 indicates significant drift requiring retraining.</p>",
      "codeBlock": "import numpy as np\nfrom scipy.stats import ks_2samp\n\ndef calculate_psi(expected, actual, num_bins=10):\n    # Helper to calculate Population Stability Index\n    breakpoints = np.percentile(expected, np.arange(0, 100, 100 / num_bins))\n    expected_cnt = np.histogram(expected, bins=breakpoints)[0] + 1e-5\n    actual_cnt = np.histogram(actual, bins=breakpoints)[0] + 1e-5\n    \n    expected_pct = expected_cnt / sum(expected_cnt)\n    actual_pct = actual_cnt / sum(actual_cnt)\n    \n    psi = np.sum((actual_pct - expected_pct) * np.log(actual_pct / expected_pct))\n    return psi\n\ndef check_ks_drift(expected, actual):\n    # Run KS-Test; small p-value (<0.05) indicates drift\n    stat, p_val = ks_2samp(expected, actual)\n    return {\"drift\": p_val < 0.05, \"p_value\": p_val}"
    },
    {
      "title": "Feature Monitoring & Feedback Loop Detection",
      "description": "<p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>psychology</span> <strong>Intuition:</strong> In recommendation and chatbot platforms, models can create positive feedback loops (e.g. recommending items similar to what the user was shown, reinforcing bias). Monitoring feature distributions detects these spirals.</p>",
      "codeBlock": "# Compute entropy of feature recommendations over time to detect polarization\nfrom scipy.stats import entropy\n\ndef detect_feedback_loop_polarization(recommended_item_counts):\n    probs = np.array(recommended_item_counts) / sum(recommended_item_counts)\n    # Low entropy indicates highly polarized recommendation distribution (potential feedback loop bias)\n    return entropy(probs)"
    },
    {
      "title": "LLM Service Level Objectives (SLOs) & Quality KPIs",
      "description": "<p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>psychology</span> <strong>Intuition:</strong> Traditional SLOs focus on server health (uptime, error rates). ML SLOs include **quality goals** (e.g. p95 Time to First Token < 300ms, refusal rate <= 4%, helpfulness score > 4/5 on hourly sampling).</p>",
      "codeBlock": "def evaluate_hourly_quality_slo(scores, threshold=4.0):\n    # Calculate percentage of samples meeting target score\n    passing = sum(1 for s in scores if s >= threshold)\n    return passing / len(scores) >= 0.95  # SLO target: 95% passing"
    },
    {
      "title": "References & Further Reading",
      "description": "<ul><li><a href='https://opentelemetry.io/docs/specs/semconv/gen-ai/'><b>OpenTelemetry GenAI Conventions</b></a></li><li><a href='https://github.com/evidentlyai/evidently'><b>Evidently AI</b></a> — Evaluation and drift library.</li></ul>",
      "codeBlock": ""
    }
  ],
  "details": "<p><strong>Staff Engineer Summary:</strong> Observability requires statistical drift monitoring. Calculate PSI and KS test statistics on sliding inference windows, track feedback loop polarization via entropy checks, and establish model quality metrics as target SLOs.</p>",
  "codeBlock": "",
  "tags": ["FAANG", "observability", "drift", "monitoring", "metrics"]
}
