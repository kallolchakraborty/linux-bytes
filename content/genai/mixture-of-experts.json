{
  "id": "genai-moe",
  "title": "Mixture of Experts \u2014 MoE Architecture, Routing & Scaling",
  "category": "Gen AI",
  "subcategory": "Architecture",
  "description": "Mixture of Experts (MoE) scales model capacity without proportional compute cost by activating only a subset of parameters per token. At Staff level, you must understand expert routing, load balancing, all-to-all communication, and when MoE beats dense models.",
  "language": "python",
  "sections": [
    {
      "title": "What is MoE? \u2014 Sparse Activation at Scale",
      "description": "<p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>psychology</span> <strong>Intuition:</strong> Instead of one FFN per transformer block, MoE replaces it with multiple 'experts' \u2014 each a small FFN. A learned <strong>router</strong> (gating network) picks the top-K experts for each token. Only the selected experts run; the rest sit idle. This decouples total parameters (capacity) from active parameters (compute).</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>bar_chart</span> <strong>Concrete numbers:</strong> Mixtral 8x7B: 47B total params, ~13B active per token (top-2 of 8 experts). Each expert is a 5.6B FFN + shared attention layers. Quality matches Llama 2 70B on most benchmarks at 5\u00d7 lower inference FLOP (13B vs 70B active). Cost ratio: 47B total is 2\u00d7 more VRAM than 7B dense, but 3\u00d7 more tokens/sec.</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>search</span> <strong>Interview Angle:</strong> \"Why not just train a 47B dense model?\" A 47B dense model costs 3.6\u00d7 more FLOP per token than Mixtral 8x7B (47B vs 13B active). At batch size 1 (chat), the dense model is 3.6\u00d7 slower. At large batch sizes, both saturate GPU compute, but the dense model still requires 3.6\u00d7 more memory bandwidth for weight reads.</p>",
      "codeBlock": "# MoE layer replacing a standard FFN\nclass MoELayer(nn.Module):\n    def __init__(self, d_model, d_ff, n_experts, top_k):\n        super().__init__()\n        self.n_experts = n_experts\n        self.top_k = top_k\n        self.router = nn.Linear(d_model, n_experts, bias=False)\n        # Each expert is a small SwiGLU FFN\n        self.experts = nn.ModuleList([\n            SwiGLUFFN(d_model, d_ff) for _ in range(n_experts)\n        ])\n\n    def forward(self, x):\n        B, T, D = x.shape\n        # Router logits: [B, T, E]\n        logits = self.router(x)\n        # Top-k routing: [B, T, k] indices + weights\n        weights, indices = torch.topk(logits.softmax(dim=-1), self.top_k, dim=-1)\n\n        out = torch.zeros_like(x)\n        for i, expert in enumerate(self.experts):\n            # Mask: which tokens route to this expert\n            mask = (indices == i).any(dim=-1)  # [B, T]\n            if mask.any():\n                expert_in = x[mask]\n                expert_out = expert(expert_in)\n                # Weighted sum: weight[i] * expert_output\n                w = weights[mask][torch.where(indices[mask] == i)[1]]\n                out[mask] += w.unsqueeze(-1) * expert_out\n        return out\n\n# Mixtral 8x7B: 8 experts, top-2, d_ff=14336 per expert\n# Total FFN params = 8 * (3 * 4096 * 14336) \u2248 1.4B per MoE layer\n# Compared to dense: 1 * (3 * 4096 * 14336) \u2248 176M\n# MoE adds 7\u00d7 more FFN params but only 2\u00d7 more compute"
    },
    {
      "title": "Routing & Top-K Gating \u2014 The Sparse Gate",
      "description": "<p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>psychology</span> <strong>Intuition:</strong> The router learns which experts are best for each token. Input token \u2192 linear projection \u2192 logits \u2192 softmax \u2192 top-K sparsity. Gating noise during training (Gaussian noise on logits) encourages exploration \u2014 without it, the router converges to sub-optimal expert assignments too early.</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>bar_chart</span> <strong>Concrete numbers:</strong> Top-2 is the empirical sweet spot. K=1 (Switch Transformer): maximum sparsity but each token gets only one expert opinion \u2014 gradient flows poorly through the single expert, and the expert collapse issue is worse. K=4+: quality approaches dense but loses the MoE compute advantage. With E=64 experts, top-2 means 97% of parameters are idle \u2014 that's the efficiency win.</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>track_changes</span> <strong>Practical Workflow:</strong> Start with noisy top-k gating: logits += N(0, 1/noise_scale) during training. Noise scale = 1.0 (Shazeer default). Anneal noise to 0 at end of training. Use K=2 for most setups. K=1 for extreme throughput needs. Never use K > 4.</p>",
      "codeBlock": "# Noisy top-k gating (Shazeer et al. 2017)\nimport torch\n\ndef noisy_top_k_gating(x, router_weight, n_experts, k=2, noise_scale=1.0, training=True):\n    \"\"\"\n    x: [batch, dim] \u2014 token representations\n    Returns: [batch, n_experts] weights (sparse)\n    \"\"\"\n    logits = x @ router_weight  # [batch, n_experts]\n\n    if training and noise_scale > 0:\n        # Gaussian noise encourages exploration\n        noise = torch.randn_like(logits) * noise_scale\n        logits = logits + noise\n\n    # Softmax over all experts\n    weights = logits.softmax(dim=-1)\n\n    # Top-k masking: keep only top-k, zero the rest\n    top_k_vals, top_k_idx = torch.topk(weights, k, dim=-1)\n    mask = torch.zeros_like(weights).scatter_(-1, top_k_idx, 1.0)\n    weights = weights * mask\n\n    # Renormalize to sum to 1 (over the k selected experts)\n    weights = weights / weights.sum(dim=-1, keepdim=True)\n    return weights, top_k_idx\n\n# ponytail: real impl uses einsum for efficiency, not explicit loop\n# Ceiling: noise anneal schedule needs tuning per architecture"
    },
    {
      "title": "Load Balancing & Auxiliary Loss \u2014 Preventing Expert Collapse",
      "description": "<p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>psychology</span> <strong>Intuition:</strong> Without intervention, the router converges to sending nearly all tokens to 1-2 experts. The other experts atrophy (never trained) \u2014 this is <strong>expert collapse</strong>. The fix: an auxiliary loss that penalizes uneven expert utilization.</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>bar_chart</span> <strong>Concrete numbers:</strong> Auxiliary loss coefficient = 0.01 (standard across MoE papers). The loss = n_experts \u00d7 \u03a3(f_i \u00d7 P_i) where f_i = fraction of tokens routed to expert i, P_i = average router probability for expert i. Uniform assignment gives loss = 1.0. Collapse (all tokens to one expert) gives loss = n_experts. Without this loss, MoE training fails within 100 steps \u2014 router logits diverge and training loss explodes.</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>search</span> <strong>Interview Angle:</strong> \"The auxiliary loss conflicts with the primary training loss \u2014 how do you balance?\" The auxiliary loss is intentionally small (0.01 coefficient). It acts as a regularizer, not a competing objective. If you see expert collapse, increase coefficient to 0.02. If you see quality degradation from over-regularization, decrease to 0.005. Z-loss (penalizing large router logits) is complementary \u2014 it keeps the router in a stable numerical range.</p>",
      "codeBlock": "# Load balancing auxiliary loss (Switch Transformer style)\ndef load_balancing_loss(router_logits, expert_indices, n_experts):\n    \"\"\"\n    router_logits: [batch, n_experts] \u2014 pre-softmax\n    expert_indices: [batch] \u2014 chosen expert per token\n    \"\"\"\n    B = router_logits.shape[0]\n\n    # f_i: fraction of tokens routed to each expert\n    f_i = torch.zeros(n_experts, device=router_logits.device)\n    f_i.scatter_add_(0, expert_indices,\n                     torch.ones(B, device=router_logits.device))\n    f_i = f_i / B\n\n    # P_i: avg router probability assigned to each expert\n    router_probs = router_logits.softmax(dim=-1)\n    P_i = router_probs.mean(dim=0)\n\n    # Loss = n_experts * sum(f_i * P_i)\n    # Uniform: f_i = 1/E, P_i = 1/E \u2192 loss = E * E * (1/E\u00b2) = 1.0\n    # Collapse: f_0 = 1, P_0 = 1 \u2192 loss = E * 1 * 1 = E\n    loss = n_experts * (f_i * P_i).sum()\n    return loss\n\n# Z-loss: penalizes large router logits for stability\n# (used in Switch Transformer + GShard)\ndef z_loss(router_logits, coefficient=1e-4):\n    \"\"\"\n    Encourage router logits to stay small (centered near 0).\n    Without this, router logits can grow to 100+ leading to NaN.\n    \"\"\"\n    return coefficient * router_logits.square().mean()\n\n# Total loss = nll + 0.01 * lb_loss + 1e-4 * z_loss"
    },
    {
      "title": "All-to-All Communication \u2014 The MoE Bottleneck",
      "description": "<p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>psychology</span> <strong>Intuition:</strong> In distributed MoE, each GPU holds a subset of experts. When a token on GPU 0 routes to an expert on GPU 3, the token must be sent (with its hidden state) to GPU 3 via <strong>all-to-all</strong> communication. After the expert processes it, the result comes back. The communication cost grows linearly with the number of experts.</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>bar_chart</span> <strong>Concrete numbers:</strong> Mixtral 8x7B on 8 GPUs with 1 expert per GPU: all-to-all overhead is ~5-10% of step time. Each token's hidden state (4096 floats) must be sent to 1-2 GPUs and received back. At 64 experts across 64 GPUs: overhead can be 30-50% \u2014 each GPU sends tokens to 2 of 64 destinations (random access pattern). Solution: overlap communication with expert computation using double-buffered all-to-all.</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>track_changes</span> <strong>Practical Workflow:</strong> Profile your all-to-all vs compute ratio. If communication dominates: 1) Increase tokens per GPU (larger batch size hides latency). 2) Use expert parallelism + tensor parallelism hybrid (GShard style). 3) Overlap: compute experts on local tokens while all-to-all for remote tokens is in-flight. 4) For >64 experts, consider expert sharding (each expert split across multiple GPUs).</p>",
      "codeBlock": "# All-to-all communication in distributed MoE (conceptual)\ndef moe_all_to_all_send_recv(hidden_states, expert_assignment, world_size, rank):\n    \"\"\"\n    hidden_states: [local_tokens, d_model] \u2014 this GPU's tokens\n    expert_assignment: [local_tokens] \u2014 expert IDs per token\n\n    All-to-all: each rank sends tokens to the rank that has their expert.\n    \"\"\"\n    # Group tokens by destination rank\n    send_bufs = [[] for _ in range(world_size)]\n    for i, expert_id in enumerate(expert_assignment):\n        dst_rank = expert_id % world_size  # expert-to-rank mapping\n        send_bufs[dst_rank].append(hidden_states[i])\n\n    # Concatenate per destination\n    send_bufs = [torch.stack(buf) if buf else torch.empty(0, hidden_states.size(-1))\n                 for buf in send_bufs]\n\n    # All-to-all communication\n    recv_bufs = [torch.empty_like(buf) for buf in send_bufs]\n    # torch.distributed.all_to_all(recv_bufs, send_bufs)\n    # ponytail: real impl uses NCCL all-to-all with CUDA streams\n\n    return recv_bufs\n\n# Communication overhead grows linearly with n_experts.\n# For Mixtral 8x7B on 8 GPUs: each GPU sends ~12.5% of tokens\n# to each of 2 remote targets (top-2 routing).\n# ponytail: for 64+ experts, use hierarchical all-to-all (torus topology)"
    },
    {
      "title": "Expert Capacity & Token Dropping \u2014 Graceful Degradation",
      "description": "<p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>psychology</span> <strong>Intuition:</strong> Each expert has a fixed slot count (<strong>capacity</strong>). If too many tokens route to the same expert, the excess tokens are <strong>dropped</strong> \u2014 their hidden state passes through unchanged (like a residual skip). Capacity prevents any single expert from becoming a bottleneck and ensures predictable compute per batch.</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>bar_chart</span> <strong>Concrete numbers:</strong> Capacity = tokens_per_batch \u00d7 capacity_factor / n_experts. Standard capacity_factor = 1.0-1.25. At 1.0, each expert gets exactly its proportional share. At 1.25, 25% overflow headroom. Drop rate target < 1%. If drop rate > 5%, increase capacity_factor. Mixtral 8x7B uses capacity_factor = 1.0 with < 0.5% drop rate in practice.</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>search</span> <strong>Interview Angle:</strong> \"Doesn't dropping tokens hurt quality?\" Yes, but < 1% drops are negligible. If dropping occurs, the token's representation is weaker (missing expert refinement) but the residual connection preserves the layer input. GShard showed that up to 5% drops don't significantly hurt downstream tasks. The alternative \u2014 no capacity limit \u2014 allows one expert to hog compute, creating unpredictable latency. Capacity + drop is the lesser evil.</p>",
      "codeBlock": "# Expert capacity enforcement with token dropping\nclass CapacityMoE(nn.Module):\n    def __init__(self, d_model, d_ff, n_experts, top_k, capacity_factor=1.0):\n        super().__init__()\n        self.n_experts = n_experts\n        self.top_k = top_k\n        self.capacity_factor = capacity_factor\n        self.router = nn.Linear(d_model, n_experts, bias=False)\n        self.experts = nn.ModuleList([\n            SwiGLUFFN(d_model, d_ff) for _ in range(n_experts)\n        ])\n\n    def forward(self, x):\n        B, T, D = x.shape\n        N = B * T  # total tokens\n        capacity = int(N * self.capacity_factor / self.n_experts)\n\n        # Get top-k routing decisions\n        weights, indices = torch.topk(\n            self.router(x).softmax(dim=-1), self.top_k, dim=-1)\n\n        out = torch.zeros_like(x)\n        for i, expert in enumerate(self.experts):\n            # Which tokens (across top-k) chose this expert?\n            positions = (indices == i).nonzero(as_tuple=True)\n            tokens, k_idx = positions[0], positions[1]\n\n            if len(tokens) == 0:\n                continue\n\n            # Capacity limit: only process first `capacity` tokens\n            if len(tokens) > capacity:\n                tokens = tokens[:capacity]\n                k_idx = k_idx[:capacity]\n                # Remaining tokens are dropped (handled implicitly)\n                # ponytail: log drop rate for monitoring\n\n            # Process selected tokens (only up to capacity)\n            expert_in = x[tokens.flatten()].view(-1, D)\n            expert_out = expert(expert_in)\n\n            # Apply routing weight\n            w = weights[tokens.flatten()].view(-1)\n            out[tokens.flatten()] += w.unsqueeze(-1) * expert_out\n\n        # Dropped tokens: their `out` stays as 0 (identity through residual)\n        return out\n\n# Monitor: if drop_rate > 0.05, increase capacity_factor\n# Re-route strategy: re-assign dropped tokens to next-best expert"
    },
    {
      "title": "MoE vs Dense \u2014 Decision Framework for Production",
      "description": "<p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>psychology</span> <strong>Intuition:</strong> MoE and dense models occupy different points on the efficiency-quality Pareto frontier. The choice depends on serving workload, latency budget, and hardware. This section provides a concrete framework to make that call.</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>bar_chart</span> <strong>When MoE wins:</strong> 1) Moderate-to-large batch serving (chat, API with batching) \u2014 MoE's active-param advantage shines. 2) Memory-constrained (mobile, edge, single GPU) \u2014 47B total but 13B active = 70B quality on 1 GPU. 3) Multi-task models \u2014 different experts can specialize for different domains (code, math, creative). 4) Training cost dominated by data parallelism \u2014 MoE's expert parallelism adds less overhead than model parallelism for dense 70B.</p><p><strong>When dense wins:</strong> 1) Very small batch (latency-critical < 50ms) \u2014 all-to-all overhead dominates. 2) Single-task, data-rich (language understanding on fixed domain) \u2014 one dense FFN is enough. 3) Hardware without fast interconnects \u2014 MoE needs NVLink or InfiniBand; on Ethernet, all-to-all kills performance.</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>track_changes</span> <strong>Hybrid approach (DeepSeek V2):</strong> First 3 layers dense (learn universal features), remaining 55 layers MoE (specialized refinement). This gives 70B-quality at ~20B active params with better training stability than pure MoE.</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>search</span> <strong>Staff Engineer Question:</strong> \"500M queries/day chatbot. Dense 70B costs $100K/mo on 16 A100s. Same budget \u2014 should you switch to MoE?\" Yes. MoE 8x7B: 47B total fits on 4 A100s (INT4), serves at 2\u00d7 throughput = 8 instances on 4 A100s. Your 16 GPUs serve 4\u00d7 the traffic at same cost. Quality is within 1-2% on chat benchmarks. MoE wins.</p>",
      "codeBlock": "# MoE vs Dense: active parameter comparison\nimport math\n\ndef moe_vs_dense(total_params_b, active_params_b, dense_params_b, batch_size):\n    \"\"\"\n    Compare FLOP per token for MoE vs dense at varying batch sizes.\n    \"\"\"\n    # Active params determine compute\n    moe_flop = active_params_b * 2  # 1 FLOP per param per token (rough)\n    dense_flop = dense_params_b * 2\n\n    # All-to-all overhead: proportional to total_params (communication)\n    # Scales with batch: more tokens = more data to transfer\n    aa_overhead = 0.05 * batch_size * total_params_b  # 5% of total per batch\n\n    print(f\"MoE   active: {active_params_b:.0f}B, total: {total_params_b:.0f}B\")\n    print(f\"Dense active: {dense_params_b:.0f}B, total: {dense_params_b:.0f}B\")\n    print(f\"Batch={batch_size}: MoE {moe_flop:.0f}G FLOP/token, \"\n          f\"Dense {dense_flop:.0f}G FLOP/token\")\n    print(f\"All-to-all overhead: {aa_overhead:.1f}G per batch\")\n    print(f\"MoE wins: {'Yes' if moe_flop + aa_overhead/batch_size < dense_flop else 'No'}\")\n\n# Compare at small vs large batch\nprint(\"=== Small batch (1) ===\")\nmoe_vs_dense(47, 13, 70, 1)\nprint(\"\\n=== Large batch (1024) ===\")\nmoe_vs_dense(47, 13, 70, 1024)\n# At batch=1, MoE compute is 13/70 = 18% of dense\n# At batch=1024, all-to-all overhead erodes some advantage\n# ponytail: exact break-even depends on interconnect bandwidth"
    },
    {
      "title": "Dense Models \u2014 When and Why to Go Dense",
      "description": "<p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>psychology</span> <strong>Intuition:</strong> Dense models use a single FFN per transformer block where <em>every parameter is active for every token</em>. There's no router, no expert selection, no all-to-all communication. What you see (parameter count) is what you pay (compute per token). This simplicity is the dense model's superpower \u2014 and its limitation.</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>bar_chart</span> <strong>When dense wins over MoE:</strong> 1) <strong>Latency-critical serving < 50ms:</strong> At batch size 1, all parameters are resident on the GPU and there's zero communication overhead. MoE must pay all-to-all latency even for a single token. 2) <strong>Single-task domains:</strong> If your model only needs to excel at one thing (code completion or sentiment analysis), a dense model's full capacity per token beats MoE's 13B/47B active split. 3) <strong>Hardware without fast interconnects:</strong> MoE on Ethernet (< 100GbE) spends 30-50% step time in all-to-all. Dense models only need model parallelism which is more bandwidth-efficient.</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>bar_chart</span> <strong>Notable dense model families:</strong> <strong>Llama 3</strong> (8B, 70B, 405B \u2014 all dense), <strong>GPT-4</strong> (1.8T alleged, 8 experts \u00d7 220B \u2014 unconfirmed; officially treated as dense for API purposes), <strong>BERT</strong> (encoder-only dense, 110M-340M), <strong>T5</strong> (encoder-decoder dense, 220M-11B). Every frontier lab has both dense and MoE models \u2014 they're complementary tools.</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>bar_chart</span> <strong>Concrete numbers \u2014 memory & hardware comparison:</strong> Llama 3 405B dense: 810GB in FP16 (405B \u00d7 2 bytes). Requires 8 \u00d7 H100 (80GB each) with tensor parallelism. Mixtral 8x7B MoE: 47B total params, 13B active, fits 2 \u00d7 H100 at FP16 or 1 \u00d7 H100 at INT4. <strong>The tradeoff:</strong> MoE gives more knowledge (total params) at less compute (active params), while dense gives <em>all</em> its parameters to every reasoning step. For math, logic, and multi-step reasoning benchmarks, dense models often outperform MoE at the same active-param budget.</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>psychology</span> <strong>The 'knowledge vs reasoning' tradeoff:</strong> MoE's total parameter count represents stored knowledge (facts, patterns, linguistic rules in different experts). Its active parameter count represents reasoning depth. Dense models, with 100% of params active per token, dedicate more FLOP to reasoning at each step. On MMLU (knowledge), MoE 8x7B \u2248 Llama 2 70B. On MATH (reasoning), dense models with the same active param count consistently lead.</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>search</span> <strong>Staff Engineer insight:</strong> \"Dense is preferred when you control the full stack and latency is the #1 metric.\" At FAANG, the choice is driven by serving economics: if your serving fleet is homogeneous (all H100, all NVLink, predictable batch sizes), MoE's complexity often isn't worth it. Dense models are simpler to deploy, easier to debug (no expert collapse, no load balancing), and have more predictable performance. MoE is the choice when you need 70B-quality on a single GPU or when training compute budget is the bottleneck.</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>search</span> <strong>Interview Angle:</strong> \"Your startup has 4 H100s and needs a coding assistant with <100ms latency. Dense or MoE?\" Use dense \u2014 4 H100s can serve Llama 3 70B with tensor parallelism (4-way TP), achieving <100ms per token. A MoE 8x7B would need all-to-all across 4 GPUs, adding 15-25ms communication overhead per token, pushing you past the latency budget. MoE's all-to-all advantage only kicks in at batch sizes > 4 or when you need 405B-quality on limited GPUs.</p>",
      "codeBlock": "# Dense FFN vs MoE forward pass comparison\nimport torch, torch.nn as nn\n\nclass DenseFFN(nn.Module):\n    \"\"\"Dense FFN: all params active for every token.\"\"\"\n    def __init__(self, d_model=4096, d_ff=14336):\n        super().__init__()\n        self.gate = nn.Linear(d_model, d_ff, bias=False)\n        self.up   = nn.Linear(d_model, d_ff, bias=False)\n        self.down = nn.Linear(d_ff, d_model, bias=False)\n\n    def forward(self, x):\n        # 100% params compute for every token\n        return self.down(F.silu(self.gate(x)) * self.up(x))\n    # FLOP per token: 2 * d_model * d_ff * 3 (gate+up+down)\n    # = 2 * 4096 * 14336 * 3 \u2248 352M FLOP\n\nclass MoEFFN(nn.Module):\n    \"\"\"MoE: only top-2 of 8 experts compute per token.\"\"\"\n    def __init__(self, d_model=4096, d_ff=14336, n_experts=8, top_k=2):\n        super().__init__()\n        self.router = nn.Linear(d_model, n_experts, bias=False)\n        self.experts = nn.ModuleList([\n            DenseFFN(d_model, d_ff) for _ in range(n_experts)\n        ])\n        self.top_k = top_k\n\n    def forward(self, x):\n        B, T, D = x.shape\n        logits = self.router(x)  # [B, T, E]\n        weights, indices = torch.topk(logits.softmax(dim=-1), self.top_k, dim=-1)\n\n        out = torch.zeros_like(x)\n        for i, expert in enumerate(self.experts):\n            mask = (indices == i).any(dim=-1)  # [B, T]\n            if mask.any():\n                w = weights[mask][torch.where(indices[mask] == i)[1]]\n                out[mask] += w.unsqueeze(-1) * expert(x[mask])\n        return out\n    # FLOP per token: 2 * d_model * d_ff * top_k (13B active)\n    # = 2 * 4096 * 14336 * 2 \u2248 235M FLOP (33% less than dense!)\n\n# Key insight: MoE uses 33% less FLOP per token at same d_ff\n# because only top-k experts compute. But total params are 8\u00d7 more.\n# ponytail: this comparison ignores attention FLOP which are identical"
    },
    {
      "title": "References & Key Papers",
      "description": "<ul><li><a href='https://arxiv.org/abs/1701.06538'><b>Outrageously Large Neural Networks: The Sparsely-Gated Mixture-of-Experts Layer</b></a> (Shazeer et al., 2017) \u2014 The original MoE paper: noisy top-k gating, auxiliary loss, 137B model with 1000 experts</li><li><a href='https://arxiv.org/abs/2006.16668'><b>GShard: Scaling Giant Models with Conditional Computation and Automatic Sharding</b></a> (Lepikhin et al., 2020) \u2014 Google's distributed MoE for 600B translation model, expert parallelism, top-2 gating at scale</li><li><a href='https://arxiv.org/abs/2101.03961'><b>Switch Transformers: Scaling to Trillion Parameter Models with Simple and Efficient Sparsity</b></a> (Fedus et al., 2022) \u2014 Switch to top-1 gating for maximum sparsity, 1.6T model, load balancing loss</li><li><a href='https://arxiv.org/abs/2401.04088'><b>Mixtral of Experts</b></a> (Mistral AI, 2024) \u2014 8x7B MoE matching Llama 2 70B quality, practical deployment guide</li><li><a href='https://arxiv.org/abs/2405.04434'><b>DeepSeek-V2: A Strong, Economical, and Efficient Mixture-of-Experts Language Model</b></a> (DeepSeek, 2024) \u2014 Hybrid dense+MoE architecture, Multi-Head Latent Attention, 236B total with 21B active</li><li><a href='https://arxiv.org/abs/2302.01126'><b>Scaling Vision Transformers with MoE</b></a> \u2014 MoE applied to ViT, scaling laws for sparse models</li></ul>",
      "codeBlock": ""
    }
  ],
  "details": "<p><strong>MoE Decision Flow \u2014 When to Use Sparse Models:</strong></p><ol><li><strong>Latency budget > 100ms?</strong> \u2192 Yes: MoE viable. No (< 50ms): prefer dense to avoid all-to-all overhead at small batch.</li><li><strong>Single GPU deployment?</strong> \u2192 MoE enables 70B-quality on 1 GPU (INT4 MoE \u2248 24GB). Dense 70B needs 2-4 GPUs.</li><li><strong>Multi-task (chat, code, math, retrieval)?</strong> \u2192 MoE experts can specialize. Dense models are less flexible per-param.</li><li><strong>Batch size > 4?</strong> \u2192 MoE's all-to-all amortizes. For batch 1 streaming, the overhead per-token is highest.</li><li><strong>Interconnect bandwidth?</strong> \u2192 NVLink 600GB/s+ \u2192 MoE is fine. Ethernet < 100GbE \u2192 dense may be faster.</li><li><strong>Cost-optimal at scale:</strong> MoE 8x7B serves 3\u00d7 more queries than dense 70B at same GPU budget (active params 13B vs 70B, 5.4\u00d7 advantage partially offset by all-to-all + total VRAM).</li></ol><p><strong>Bottom line:</strong> MoE is the default choice for deployed LLM serving at scale (millions of queries/day). Dense is for latency-critical edge cases and research simplicity. The industry trajectory points toward hybrid architectures (DeepSeek V2, Qwen2-MoE) that use dense early layers for universal features and MoE later layers for specialized refinement.</p>",
  "codeBlock": "",
  "tags": [
    "FAANG",
    "gen-ai",
    "moe",
    "architecture",
    "routing",
    "scaling",
    "mixture-of-experts"
  ]
}