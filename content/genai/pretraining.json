{
  "id": "genai-pretraining",
  "title": "Pre-Training LLMs at Scale — Data, Recipes & Distributed Training",
  "category": "Gen AI",
  "subcategory": "Training",
  "description": "Pre-training is the most compute-intensive phase — costing $10M-100M for frontier models. At Staff Engineer level, you must design data pipelines, optimize distributed training, and debug training stability issues at scale.",
  "language": "python",
  "sections": [
    {
      "title": "The Data Pipeline: From Raw Web to Training Examples",
      "description": "<p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>psychology</span> <strong>Intuition:</strong> The internet is full of noise — ads, boilerplate, duplicates, PII. Pre-training data quality often matters more than quantity. A 10% improvement in data quality can match 2× more data.</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>bar_chart</span> <strong>Concrete numbers:</strong> Common Crawl monthly dump ≈ 80TB compressed. After dedup + quality filtering → ~2TB usable text. FineWeb (HuggingFace): 15T tokens of aggressively deduplicated web data. FineWeb-Edu: subset filtered for educational value → matches 2× more unfiltered data on academic benchmarks.</p><p><strong>Processing stages:</strong> 1) Crawl (Common Crawl, WARC files) 2) Language detection (fastText) 3) Quality filtering (perplexity threshold, Gopher rules, C4 heuristics) 4) Deduplication (MinHash LSH at URL level, exact match at document level, subdocument dedup at 50-gram overlap) 5) PII removal (regex + NER for emails, IPs, SSNs) 6) Toxic content filtering (classifier-based) 7) Decontamination (remove eval set n-gram overlaps)</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>track_changes</span> <strong>Practical Workflow:</strong> For a $10M pre-training run: Spend $500K on data curation. Train a small proxy model (1B params) on each candidate data mix. Evaluate on target benchmarks. Choose the best mix. Then scale to full 70B training.</p>",
      "codeBlock": "# MinHash LSH for near-deduplication at scale\nimport hashlib\n\ndef minhash_shingles(text, k=5, bands=10):\n    shingles = set(text[i:i+k] for i in range(len(text)-k+1))\n    hashes = [hashlib.md5(s.encode()).hexdigest() for s in shingles]\n    min_hashes = [min(hashes[i::bands]) for i in range(bands)]\n    return tuple(min_hashes)\n\ndef dedup_corpus(documents, threshold=0.7):\n    buckets = {}\n    for doc_id, doc in enumerate(documents):\n        sig = minhash_shingles(doc)\n        for band_idx, h in enumerate(sig):\n            key = (band_idx, h)\n            buckets.setdefault(key, set()).add(doc_id)\n    dup_pairs = set()\n    for bucket in buckets.values():\n        for i in bucket:\n            for j in bucket:\n                if i < j:\n                    dup_pairs.add((i, j))\n    return dup_pairs\n"
    },
    {
      "title": "Data Mixing: The Secret Sauce",
      "description": "<p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>psychology</span> <strong>Intuition:</strong> Different data sources contribute different capabilities. Web text teaches general knowledge, books teach narrative coherence, code teaches reasoning, math teaches precision. The mixing ratio determines the model's capability profile.</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>bar_chart</span> <strong>Concrete recipe (Llama 3):</strong> ~50% general web + code, ~25% math & reasoning (including synthetic math data), ~17% books & academic papers, ~8% code from GitHub. FineWeb-Edu for quality-filtered web. Llama 3 also used <em>synthetic data</em> for math (code-interpreter-generated solutions) — ~1T tokens.</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>track_changes</span> <strong>Practical Workflow:</strong> 1) Start with a baseline mix (e.g., the Pile proportions). 2) Train small models (1B-7B) on different mixes. 3) Evaluate on 20+ diverse benchmarks (MMLU, HellaSwag, ARC, GSM8K, HumanEval). 4) Adjust based on target use case — more code for coding models, more math for reasoning models.</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>search</span> <strong>Interview Angle:</strong> \"How do you decide the data mix for a new LLM?\" Start with the Pile proportions as baseline. Upsample code and math by 2-3× for reasoning capability. Add synthetic data (code-interpreter-generated math solutions, model-generated QA pairs). Use proxy models to evaluate candidate mixes before full training.</p>",
      "codeBlock": "# Data mixing experiment framework\nimport numpy as np\n\ndef evaluate_data_mix(mix_proportions, proxy_results):\n    target_benchmarks = ['mmlu', 'humaneval', 'gsm8k', 'hellaswag', 'arc']\n    scores = {}\n    for b in target_benchmarks:\n        domain_scores = [proxy_results[d].get(b, 0) for d in mix_proportions]\n        scores[b] = np.average(domain_scores, weights=list(mix_proportions.values()))\n    return scores\n\nmixes = [\n    {'web': 0.7, 'code': 0.1, 'books': 0.15, 'math': 0.05},\n    {'web': 0.5, 'code': 0.2, 'books': 0.15, 'math': 0.15},\n    {'web': 0.4, 'code': 0.25, 'books': 0.1, 'math': 0.25},\n]\nfor i, mix in enumerate(mixes):\n    print(f\"Mix {i}: {mix}\")\n"
    },
    {
      "title": "The Scaling Law: Predict Optimal Model Size for Budget",
      "description": "<p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>bar_chart</span> <strong>Concrete numbers:</strong> Given a compute budget C, Chinchilla says optimal N ∝ C⁰·⁵ and D ∝ C⁰·⁵. But this assumes you only care about perplexity. For downstream task performance, the exponents differ — models with more tokens (lower D:N ratio) do better on knowledge benchmarks.</p><p><strong>Key formula:</strong> <code>L(N,D) = E + A/N^alpha + B/D^beta</code> where E ≈ 1.7 (irreducible loss), alpha ≈ beta ≈ 0.34, A ≈ 400, B ≈ 400 for Llama-like architectures.</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>track_changes</span> <strong>Practical Workflow — Budget allocation:</strong> $10M budget → ~500K A100-hours. Options: a) 7B model on 15T tokens (Llama 3 style), b) 30B model on 3.3T tokens (Chinchilla optimal). Option (a) gives better inference throughput (7B is faster) but lower quality ceiling. Option (b) gives higher quality per FLOP but worse inference cost. The Staff Engineer knows to optimize for total cost of ownership (training + inference over 1 year).</p>",
      "codeBlock": "# Predict loss from scaling law\ndef predict_loss(N, D, E=1.7, A=400, B=400, alpha=0.34, beta=0.34):\n    return E + A / (N ** alpha) + B / (D ** beta)\n\ncandidates = [\n    (7e9, 15e12, \"Llama 3 8B style\"),\n    (30e9, 3.3e12, \"Chinchilla optimal\"),\n    (70e9, 1.4e12, \"Llama 2 70B style\"),\n]\nfor N, D, name in candidates:\n    loss = predict_loss(N, D)\n    gflops = 6 * N * D\n    print(f\"{name:30s}: loss={loss:.3f}, compute={gflops:.2e} FLOP\")\n"
    },
    {
      "title": "Distributed Training: 3D Parallelism & the Communication Wall",
      "description": "<p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>psychology</span> <strong>Intuition:</strong> A 70B model doesn't fit on one GPU. You need to split parameters across many GPUs and coordinate training. The 3 dimensions of parallelism are: <strong>Data Parallel</strong> (replicate model, split data), <strong>Tensor Parallel</strong> (split each layer's weights across GPUs), <strong>Pipeline Parallel</strong> (split layers across GPUs).</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>bar_chart</span> <strong>Concrete numbers:</strong> Training a 70B model on 256 A100 80GB GPUs: Data Parallel (DP=64, each has full model) doesn't work (140GB > 80GB). Need combined parallelism. Common recipe: TP=8 (split each weight matrix 8 ways) + PP=4 (4 pipeline stages) + DP=8 (8 data replicas) = 256 GPUs. Communication: Tensor parallel requires all-reduce per layer (fast on NVLink), pipeline parallel requires point-to-point P2P (slower), data parallel requires gradient all-reduce (overlapping with backward pass).</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>track_changes</span> <strong>Practical Workflow:</strong> Choose parallelism strategy: If model fits in 1 node (8 GPUs): use FSDP (Fully Sharded Data Parallel). If model > 1 node: use TP=8 within node (NVLink bandwidth is high), PP across nodes (network bandwidth is lower). Always: compute/communication ratio matters — larger batch sizes improve it.</p>",
      "codeBlock": "# Modeling communication cost for distributed training\ndef compute_training_throughput(num_gpus, gpu_flops, model_params, \n                                 batch_seq_len, tp_size, pp_size):\n    params_per_gpu = model_params / (tp_size * pp_size)\n    tp_comm_cost = 2 * model_params * 2\n    pp_comm_cost = 4 * params_per_gpu * tp_size\n    dp_comm_cost = params_per_gpu * 4\n    total_comm = (tp_comm_cost + pp_comm_cost + dp_comm_cost)\n    return params_per_gpu, total_comm\n\nprint(compute_training_throughput(256, 312, 70e9, 4096 * 256, 8, 4))\n"
    },
    {
      "title": "FSDP: Fully Sharded Data Parallelism — ZeRO Stages",
      "description": "<p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>psychology</span> <strong>Intuition:</strong> FSDP (implemented in PyTorch) shards model parameters, gradients, and optimizer states across GPUs. You communicate only when you need the full parameter for a forward/backward pass. This is the simplest way to train large models with minimal code changes.</p><p><strong>ZeRO Stages (DeepSpeed):</strong> ZeRO-1: shard optimizer states only. ZeRO-2: shard optimizer + gradients. ZeRO-3: shard optimizer + gradients + parameters. Each stage saves more memory but requires more communication.</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>bar_chart</span> <strong>Concrete numbers:</strong> 7B model training: ZeRO-2 needs ~60GB per GPU (8× A100 80GB). ZeRO-3 needs ~30GB but 1.5× slower due to parameter all-gather every layer. For 70B: ZeRO-3 with CPU offload enables training on 128× A100 80GB (without TP/PP).</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>track_changes</span> <strong>Practical Workflow:</strong> Up to 7B: ZeRO-2 (simpler, faster). 13B-30B: ZeRO-3. 70B+: ZeRO-3 + CPU offload, or better: 3D parallelism with FSDP + Tensor Parallel + Pipeline Parallel.</p>",
      "codeBlock": "# Memory estimation for ZeRO stages\ndef zero_memory(model_params, optimizer_states=2, zero_stage=3, num_gpus=8):\n    param_bytes = model_params * 2\n    grad_bytes = model_params * 2\n    opt_bytes = model_params * optimizer_states * 4\n    if zero_stage == 1:\n        total = param_bytes + grad_bytes + (opt_bytes / num_gpus)\n        desc = \"ZeRO-1: optimizer states sharded\"\n    elif zero_stage == 2:\n        total = param_bytes + (grad_bytes / num_gpus) + (opt_bytes / num_gpus)\n        desc = \"ZeRO-2: optimizer + gradients sharded\"\n    elif zero_stage == 3:\n        total = (param_bytes / num_gpus) + (grad_bytes / num_gpus) + (opt_bytes / num_gpus)\n        desc = \"ZeRO-3: all sharded\"\n    return total / 1e9, desc\n\nprint(zero_memory(7e9, zero_stage=1))\nprint(zero_memory(7e9, zero_stage=2))\nprint(zero_memory(7e9, zero_stage=3))\n"
    },
    {
      "title": "Megatron-LM: Tensor & Pipeline Parallelism",
      "description": "<p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>psychology</span> <strong>Intuition:</strong> Megatron-LM (NVIDIA) splits individual layer computations across GPUs. Tensor Parallelism: each GPU computes a slice of the QKV/FFN matrix and communicates results. Pipeline Parallelism: different GPUs handle different layer ranges, passing activations forward and gradients backward.</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>bar_chart</span> <strong>Concrete numbers:</strong> GPT-3 (175B) was trained on 10K V100 GPUs using Megatron-LM with TP=8 + PP=64. Training took ~34 days. A single all-reduce for a 175B attention layer (TP=8) sends ~7MB per step — negligible on NVLink (600GB/s). Pipeline bubble is the main inefficiency: at PP=64, the bubble (idle time) is ~30% of total training time.</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>track_changes</span> <strong>Practical Workflow:</strong> Use Megatron-LM for 175B+ models. Recipe: TP=8 within a node (uses NVLink), PP = num_gpus / 8 for across nodes. Minimize pipeline bubble by: using micro-batches (gradient accumulation), balancing layers evenly across stages, and using interleaved schedule (1F1B, one-forward-one-backward).</p>",
      "codeBlock": "# Pipeline parallel bubble calculation\ndef pipeline_bubble(pp_size, num_microbatches):\n    return (pp_size - 1) / num_microbatches\n\nprint(f\"PP=8, micro=16: bubble={pipeline_bubble(8, 16)*100:.0f}%\")\nprint(f\"PP=8, micro=64: bubble={pipeline_bubble(8, 64)*100:.0f}%\")\nprint(f\"PP=64, micro=256: bubble={pipeline_bubble(64, 256)*100:.0f}%\")\n\n#  Staff tip: always keep bubble < 15% by increasing microbatches.\n"
    },
    {
      "title": "Flash Attention: Fused Kernel for Fast Attention",
      "description": "<p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>psychology</span> <strong>Intuition:</strong> Standard attention materializes the N×N attention matrix (O(N²) memory). FlashAttention computes attention in tiles, never writing the full matrix to HBM. It's 2-3× faster and uses <strong>no extra memory</strong> for the attention scores.</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>bar_chart</span> <strong>Concrete numbers:</strong> FlashAttention-2 achieves ~120 TFLOPS on A100 for 4K context (vs ~35 TFLOPS for standard attention). For 128K context, it's 10× faster because standard attention runs out of SRAM and spills to HBM. FlashAttention-3 (Hopper/Blackwell) adds FP8 support and async GPU overlaps.</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>track_changes</span> <strong>Practical Workflow:</strong> Always use FlashAttention-2 for training. For FP8 training: use FlashAttention-3 with FP8 matmul (available on H100+). Never use standard attention for any context length > 2K.</p>",
      "codeBlock": "# FlashAttention vs standard attention memory\nBLOCK_SIZE = 128\n\ndef attention_memory(N):\n    std_mem = N * N * 2 / 1e9\n    flash_mem = BLOCK_SIZE * BLOCK_SIZE * 2 / 1e9\n    return std_mem, flash_mem\n\nfor N in [2048, 8192, 32768, 131072]:\n    std, flash = attention_memory(N)\n    print(f\"N={N:7d}: std={std:.2f}GB, flash={flash:.4f}GB, ratio={std/flash:.0f}x\")\n"
    },
    {
      "title": "Training Stability: Loss Spikes, Gradient Clipping & Monitoring",
      "description": "<p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>psychology</span> <strong>Intuition:</strong> Training a 70B+ model can fail silently — a sudden loss spike can destroy days of compute. The root cause is usually a combination of bad data batches and optimizer state divergence.</p><p><strong>Common failure modes:</strong> 1) <strong>Loss spikes</strong>: sudden jump in loss, typically from Adam epsilon being too small or a corrupted data batch. 2) <strong>Gradient explosion</strong>: gradient norm exceeds threshold. 3) <strong>Logit drift</strong>: output logits grow unbounded. 4) <strong>Attention collapse</strong>: all attention heads converge to same pattern.</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>bar_chart</span> <strong>Concrete numbers:</strong> Gradient clipping at global norm 1.0 prevents most loss spikes. Adam epsilon=1e-8 is safe for fp16, epsilon=1e-12 for bf16 (bf16 has same exponent range as fp32). Loss spikes occur roughly every 100K-500K steps at 70B scale (1-2 per run).</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>track_changes</span> <strong>Practical Workflow:</strong> 1) Monitor: loss, gradient norm (across ALL GPUs, not just mean — the max matters), parameter norm, learning rate, tokens per second. 2) Log at batch-level to pinpoint bad data. 3) Save checkpoints every 1000-5000 steps. 4) On loss spike: reload pre-spike checkpoint, skip the bad batch, reduce LR by 50% for 100 steps, then restore.</p>",
      "codeBlock": "# Training stability monitoring (pseudo-code for distributed training)\nimport torch\n\ndef monitor_training(model, batch, global_step):\n    loss = model(batch)\n    loss.backward()\n    grad_norms = []\n    for p in model.parameters():\n        if p.grad is not None:\n            grad_norms.append(p.grad.norm().item())\n    total_norm = sum(grad_norms) ** 0.5\n    max_grad = max(grad_norms) if grad_norms else 0\n    log_metrics = {\n        'loss': loss.item(),\n        'grad_norm': total_norm,\n        'max_grad': max_grad,\n        'param_norm': sum(p.norm().item()**2 for p in model.parameters())**0.5,\n    }\n    if total_norm > 10.0:\n        print(f\"Gradient spike at step {global_step}: {total_norm:.2f}\")\n    torch.nn.utils.clip_grad_norm_(model.parameters(), 1.0)\n    return log_metrics\n"
    },
    {
      "title": "Checkpointing & Recovery at Scale",
      "description": "<p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>psychology</span> <strong>Intuition:</strong> A 70B model running for 30 days on 256 GPUs can fail at any point (GPU ECC error, NCCL timeout, OOM, power). Checkpoints are the only safety net. But checkpointing a 70B model takes non-trivial time — every checkpoint is ~280GB of weights + ~560GB of optimizer states.</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>bar_chart</span> <strong>Concrete numbers:</strong> Writing 840GB at 2GB/s (NVMe RAID) takes ~7 minutes. During this time, training must pause. At 256 A100s × $2/hr = $512/hr, each checkpoint costs ~$60 in idle compute. At 5 checkpoints/day = $300/day in checkpoint overhead. <strong>Async checkpointing</strong> (write from CPU memory while training continues) eliminates this cost.</p><p><span class='material-symbols-outlined text-sm align-middle text-slate-400' aria-hidden='true'>track_changes</span> <strong>Practical Workflow:</strong> 1) Save asynchronously to CPU memory first (sub-second). 2) Write to persistent storage from CPU in background. 3) Always save optimizer states (without them, you lose hours of training progress). 4) Keep the last 3 checkpoints + one per 5000 steps for long-term recovery. 5) Validate checkpoint integrity on save (checksum).</p>",
      "codeBlock": "# Async checkpoint coordinator (simplified)\nimport threading\nimport queue\n\ndef async_checkpoint(save_fn, model_state, opt_state, step):\n    \"\"\"Kicks off checkpoint save in background thread.\"\"\"\n    def _save():\n        torch.save(model_state, f\"ckpt-{step}-model.pt\")\n        torch.save(opt_state, f\"ckpt-{step}-optim.pt\")\n        print(f\"Checkpoint {step} saved to disk\")\n    thread = threading.Thread(target=_save, daemon=True)\n    thread.start()\n    return thread\n\n# Usage: training loop\nfor step in range(total_steps):\n    train_step(data_loader)\n    if step % 1000 == 0:\n        ckpt_thread = async_checkpoint(\n            torch.save, model.state_dict(), optimizer.state_dict(), step\n        )\n        threads.append(ckpt_thread)\n# Wait for all checkpoints before exit\nfor t in threads:\n    t.join()\n"
    },
    {
      "title": "References & Further Reading",
      "description": "<ul><li><a href='https://arxiv.org/abs/1910.02054'><b>Megatron-LM: Training Multi-Billion Parameter Language Models Using Model Parallelism</b></a> (NVIDIA, 2019)</li><li><a href='https://arxiv.org/abs/1911.02150'><b>ZeRO: Memory Optimizations Toward Training Trillion Parameter Models</b></a> (Microsoft, 2019)</li><li><a href='https://arxiv.org/abs/2104.04473'><b>Efficient Large-Scale Language Model Training on GPU Clusters Using Megatron-LM</b></a></li><li><a href='https://arxiv.org/abs/2305.07704'><b>FlashAttention-2: Faster Attention with Better Parallelism and Work Partitioning</b></a> (Dao et al., 2023)</li><li><a href='https://arxiv.org/abs/2209.05456'><b>Simple and Scalable Strategies to Continually Pre-train Large Language Models</b></a></li><li><a href='https://arxiv.org/abs/2306.01116'><b>Dolma: an Open Corpus of Three Trillion Tokens for Language Model Pretraining Research</b></a></li></ul>",
      "codeBlock": ""
    }
  ],
  "details": "<p class='mb-3'><strong>Staff Engineer Summary:</strong> Pre-training at scale is an infrastructure engineering challenge as much as a machine learning one. The top 3 risks are: <strong>data contamination</strong> leaking into eval sets (decontaminate by n-gram overlap, remove eval benchmark data from training), <strong>loss spikes</strong> (monitor all-reduce max across GPUs, not just mean), and <strong>optimizer state memory</strong> for large models (use ZeRO-3 or CPU offload for >30B params). The 5 most important data processing decisions are: quality filtering threshold, dedup strategy, domain mixing ratio, tokenizer vocab size, and synthetic data inclusion.</p><p class='mb-3'><strong>FAANG Interview Tip:</strong> When asked 'How would you train a 100B+ model?' walk through: 1) Data pipeline (C4/FineWeb quality, MinHash dedup, domain mixing), 2) Parallelism strategy (FSDP Z3 for &lt;30B, Megatron 3D for &gt;30B), 3) Training stability (gradient clipping, loss spike handling, checkpointing), 4) Scaling law verification (proxy experiments before full run). The best answer demonstrates cost awareness — 'At $10M per run, I would spend 10% on data quality, 10% on proxy experiments, and 80% on the full run.'</p>",
  "codeBlock": "",
  "tags": ["FAANG", "gen-ai", "llm", "training", "distributed"]
}
